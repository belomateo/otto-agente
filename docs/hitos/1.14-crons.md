Hito: Crons de PROCESOS.md § 2     Claude Code: logica     Fase: 1
Toca: supabase/migrations/0021_cron_envios.sql (envios_programados con unique(tipo, referencia),
  turnos.devuelto_at, las funciones de envío y la confirmación por botón) y 0022_cron_jobs.sql
  (los tres cron.schedule); supabase/functions/cron-envios/** (una sola función para los tres
  tipos, en vez de una por cron); supabase/functions/worker/** (el botón "Confirmo");
  supabase/functions/_shared/whatsapp/** (plantillas, botones, ventana y enviarPlantilla); tests
Depende de: 1.11 (envío por _shared/whatsapp). Plantillas aprobadas por Meta:
  recordatorio_turno_18h (botones "Confirmo" / "Necesito reprogramar"),
  agradecimiento_resena, recontacto_turno_pendiente, con los textos de
  docs/plantillas-whatsapp.md (las carga Mateo, decisión #11). El link de reseñas de Google en
  Configuración › Enlaces: un enlace cuyo nombre empiece con "Reseña".
Control:
  1. Recordatorio: solo sale para turnos a 24 hs sin recordatorio; marca
     `recordatorio_enviado_at`. Test: correr el cron dos veces → un solo envío.
  2. Confirmación: `confirmado=true` SOLO al llegar la respuesta al botón de la
     plantilla. Test con el payload del botón (marca) y con el texto libre "confirmo"
     (no marca: eso lo maneja Lucía). Código puro, cero LLM.
  3. Post-devolución: agradecimiento + pedido de reseña al día siguiente de marcar
     'devolvio'. Test de no reenvío.
  4. Recontacto de consulta sin turno: día siguiente y 72 hs, una vez cada uno. Si el
     cliente agendó en el medio, no sale. Test de no reenvío.
  5. Fuera de la ventana de 24 hs de WhatsApp solo se mandan plantillas, nunca texto
     libre (verificado en código antes de enviar).
  6. Cada envío deja fila en `eventos_agente`. `deno check` limpio y tests verdes.
Evidencia: 15/9 — hecho y desplegado, con los envíos apagados hasta que Meta apruebe las
  plantillas. 0021 y 0022 se probaron dos veces en una transacción con rollback y se aplicaron;
  los tres crons quedaron activos: envios-recordatorio cada 15 minutos, envios-agradecimiento a
  las 11:00 y envios-recontacto a las 11:05 (hora de Argentina). cron-envios y worker
  desplegadas, con NEGOCIO_TZ cargada y CRONS_ENVIOS sin cargar: la función contesta
  {apagado: true} y no manda nada.
  1. npm test [1.14]: sale para el turno sin confirmar de mañana que entró en las 18 hs; no para
     uno a 25 hs, uno confirmado ni uno reservado hace menos de 24 hs. El mismo envío reservado
     dos veces: la segunda reserva no sale (unique en la base). Al salir quedan
     recordatorio_enviado_at, el mensaje en la charla del cliente y el evento; correr el cron
     otra vez no lo repite.
  2. Solo el botón confirma: payload CONFIRMO:<turno> → confirmado, con confirmado_por =
     'cliente'; el texto "confirmo" no (deno test y npm test). El botón de otro cliente no
     confirma un turno ajeno, tocarlo dos veces no cambia nada, y con la charla derivada el botón
     se encola igual. De punta a punta contra lo desplegado (tests/sql/envios-desplegado.mjs):
     con la charla derivada, el worker confirmó el turno, dejó el evento boton-confirmo y no le
     contestó al número de prueba (envíos apagados y fuera de la lista).
  3. devuelto_at lo pone un trigger al marcar 'devolvio'. El agradecimiento sale al día
     siguiente, no el mismo día ni pasada una semana, y no se repite.
  4. Recontacto 1 al día siguiente de la consulta; no si agendó ni si la charla la tiene una
     persona. Recontacto 2 a los tres días, solo si salió el primero. Ninguno se repite.
  5. puedeTextoLibre se chequea en código antes de cada texto libre del worker (deno test:
     dentro de las 24 hs sí, justo en el límite no, sin mensaje del cliente no). Los crons solo
     mandan plantillas.
  6. Cada envío deja su evento en la bitácora; con Meta caída reintenta hasta 3 veces y después
     no insiste. `deno check` limpio en worker, cron-envios, webhook-whatsapp y
     _shared/whatsapp; `deno test` de _shared/whatsapp 20/20; `npm test` todo verde (16
     controles nuevos); envios-desplegado.mjs 8/8, sin restos en la base.
  Falta para cerrarlo: el envío real cuando Meta apruebe las tres plantillas. Cargar el link de
  reseñas en Enlaces, poner CRONS_ENVIOS=on y probarlo con el número de Mateo: recordatorio →
  tocar "Confirmo" → el turno queda confirmado en el panel.
  Superado el 16/9 (decisión de Mateo): el control 2 de acá arriba (confirmación SOLO por botón)
  queda sin vigencia. Se saca el botón "Confirmo"; confirma la herramienta `confirmar_turno`
  (agente) cuando Lucía entiende la intención del cliente, venga como venga. Reusa
  `turno_confirmar_por_boton` (0021, esta misma migración) — no se tocó el nombre de la función
  ni el camino del botón en `worker/atender.ts`, que sigue siendo de logica: coordinar antes de
  sacarlo. Ver docs/supuestos.md #30.
Supuestos: #9, #10 y #11 de docs/supuestos.md, y #26 a #30 (cuándo sale cada envío, sin
  nombre no sale, el agradecimiento hasta una semana, el texto con que se contesta "Confirmo").
