Hito: Queries y route handlers por pestaña (H1.8)            Claude Code: paneles          Fase: 1
Toca: panel/lib/queries/<pestaña>.ts, panel/lib/supabase/admin.ts (cliente con service role,
  solo servidor), panel/lib/tipos-db.ts (tipos generados), panel/app/api/<pestaña>/**,
  panel/middleware.ts
Depende de: Fase 0 (tablas y RLS). Las columnas nuevas de clientes (logica 1.15) y de turnos
  (0011, este rol) cuando existan.
Control:
  1. Una función de consulta tipada por pestaña (Bandeja, Atención humana, Turnos, Clientes,
     Conocimiento, Catálogo, Bitácora, Configuración) que devuelve lo mismo que muestra hoy la
     maqueta de front, con la misma forma que sus mocks. Tipos generados de la base.
  2. La service role solo existe en panel/lib/supabase/admin.ts y en panel/app/api/**:
     grep en el código, y después de `npm run build` el prefijo de la key no aparece en
     .next/static (0 resultados).
  3. /api/** sin sesión responde 401 en JSON, no un redirect a /login (decisión pendiente del
     informe del verificador § 5, se resuelve acá).
  4. Con el admin de prueba, cada handler responde 200 con datos; con una cuenta pendiente,
     0 filas o 403.
  5. `npm run typecheck` y `npm run build` limpios.
Evidencia: 13/9 — cerrado. Arnés HTTP contra el panel (next start sobre el build) y la base
  real: 118/118 aserciones entre 1.8, 1.9 y 1.10, con usuarios temporales y datos marcados que se
  borran al terminar (verificado: 0 restos, y las filas reales tocadas quedan con su versión y su
  autor originales).
  1. panel/lib/queries/: bandeja, atencion, turnos, clientes, conocimiento, catalogo, bitacora y
     configuracion. Cada una está tipada contra panel/lib/tipos-db.ts (`supabase gen types` contra
     el proyecto real con 0011–0019 aplicadas) y usa los tipos de lib/mock-data.ts, así que el
     typecheck obliga la forma de los mocks. Una ruta GET por pestaña en panel/app/api/.
  2. SUPABASE_SERVICE_ROLE_KEY la lee solo lib/supabase/admin.ts, y crearClienteAdmin la usa solo
     app/api/accesos/route.ts. Después de `npm run build`: 0 apariciones en .next/static de la
     clave completa, de su firma y del tramo del payload que la distingue de la anon. Un primer
     chequeo con los primeros 24 caracteres del payload dio 1 aparición: ese tramo es igual en la
     anon, que va al navegador a propósito.
  3. Sin sesión: 401 en JSON en las 14 rutas GET y en un PATCH. Las páginas siguen redirigiendo a
     /login. Con sesión y perfil no aprobado, 403; si no se pudo leer el perfil, 503.
  4. Con un admin, las 8 pestañas dan 200 con los datos sembrados en la forma del mock. Por
     ejemplo, Turnos devuelve «10:00 | Invitado · 45’ | Probador 3 | Sin confirmar» con el horario
     y los probadores de las tablas. Con una cuenta pendiente, 403 en las 14 rutas. El admin es una
     cuenta temporal creada por la API de Auth, no la de Mateo: no hizo falta escribir credenciales.
  5. `npm run typecheck` limpio. `npm run build` limpio: compila 36 rutas de API. Quedan dos avisos
     ajenos a este hito: la raíz del workspace por los dos lockfiles, y middleware → proxy.
Supuestos: las pestañas siguen mostrando el mock hasta Fase 2; acá se construyen y prueban
  las consultas solas (TRABAJO.md § 1).
  Además: `derivaciones` no tiene la columna `resumen` que pide PROCESOS.md § 4, así que la tarjeta
  de Atención humana muestra el último mensaje del cliente hasta que exista (logica/agente). El
  buscador de Conocimiento es provisorio hasta que exista la consulta de buscar_informacion
  (agente). La Bitácora lee `resumen`, `texto` o `mensaje` y `barandilla` del detalle de
  eventos_agente. El costo de OpenAI se muestra en US$ (consumo_llm.costo_usd), no en pesos como
  el mock. Los scripts de prueba (el arnés HTTP y la prueba en seco de cada migración) esperan
  permiso para tests/paneles/ y su línea en `npm test`, porque package.json es de logica.
