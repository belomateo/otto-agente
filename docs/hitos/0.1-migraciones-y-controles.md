Hito: Migraciones, seeds y controles de Fase 0     Claude Code: logica          Fase: 0
Toca: supabase/migrations/**, supabase/seeds/**, tests/sql/**, package.json (raíz), docs/supuestos.md
Depende de: nada
Control: `npm test` (tests/sql/run.mjs) contra el proyecto real de Supabase verifica en una
  sola corrida: (1) idempotencia del webhook — insertar dos veces el mismo wa_message_id
  rechaza el segundo por unique_violation y queda 1 solo mensaje; (2) cola — dos workers
  concurrentes llamando a cola_tomar_uno() nunca se llevan el mismo trabajo (FOR UPDATE
  SKIP LOCKED real, dos conexiones separadas); (3) RLS — anon y un usuario autenticado sin
  perfil aprobado ven 0 filas en clientes y en turnos.
Evidencia: Las 10 migraciones (0001–0010) se aplicaron sin error contra el proyecto real
  (fhyuvdliwppqkneddezn). `npm test` → "✅ Todos los controles de Fase 0 pasaron." (3/3).
  Verificado que las pruebas no dejan datos colgados (conteos en 0 después de correr).
  Seeds reales cargados desde docs/ficha-del-negocio.md: horarios (Lun–Vie 10–19 corte
  14–15, Sáb 9:30–18:30), accesorios (camisa+corbata $33.500, zapato+cinto $55.000), 6
  reglas numeradas, contexto (presentación/tono/ancla de valor), enlaces.
Supuestos: #13 (perfiles se crea en 0007, no en 0010), #14 (turnos.estado en kebab-case
  para no romper la UI del panel ya construida), #15 (catalogo_alquiler sin seed: no hay
  modelos/precios reales todavía), #16 (el proyecto de Supabase vive en la cuenta de
  Sofia, no en la de Mateo), #17 (worker por trigger + cron de 1 min de contención, no
  poll de 10 s) — todos en docs/supuestos.md.
