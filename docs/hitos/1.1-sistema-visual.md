Hito: Sistema visual (H1.1)            Claude Code: front          Fase: 1
Toca: panel/app/(panel)/componentes/page.tsx (nuevo: galería de verificación, solo en dev);
  panel/components/ui-otto/Bitacora.tsx y Burbuja.tsx + panel/app/(panel)/bandeja/ChatThread.tsx
  (la bitácora se pliega y despliega desde la «i»; mini resumen también en Ladrillo);
  panel/components/ui-otto/BloqueTurno.tsx (decisión #2: estado y aviso por separado, más los
  estados cancelado y no-vino); los textos de menos de 14 px en mobile, en las 8 pestañas
  (decisión de Mateo, 13/9). Se revisan sin tocar, salvo que el control encuentre un desvío:
  panel/tailwind.config.js, panel/app/globals.css, panel/app/layout.tsx (fuentes).
Depende de: nada (tokens y componentes ya implementados desde el canvas de Claude Design,
  antes de Fase 0). Para cerrarlo: el usuario admin de prueba (lo tiene Mateo).
Control: lo puede hacer cualquiera en panel/ de este worktree, logueado con el admin de prueba.
  1. `npm install`, `npm run typecheck` y `npm run build` terminan con código 0.
  2. Tokens: panel/tailwind.config.js tiene los valores de DISENO.md § Paleta y § Tono visual:
     hueso #F6F3EE, lino #FFFFFF, borde #E6E1D8, tinta #171A1F, grafito #5C6068,
     cobre #A8703F, cobre claro #F1E6D9, noche #1F2A3C, noche suave #EEF1F5,
     salvia #5E7F62 / #E7EFE7, ámbar #B8862B / #F7EFDD, ladrillo #A6473A / #F6E3DF;
     rounded-otto = 8px; shadow-otto = 0 1px 2px rgba(0,0,0,.06); h-fila = 56px.
     Ningún otro color con nombre salvo los agregados del canvas que figuran en Supuestos.
  3. `npm run dev`, abrir /componentes a 1440×900 y a 390×844 (DevTools, modo dispositivo).
     Se ven los 10 componentes de DISENO.md § Componentes, cada uno rotulado, con estos estados:
     - Chip: Confirmado, Sin confirmar, Urgente, Lucía, Persona, Cerrada.
     - Ficha de cliente: compacta (una línea + «Ver ficha») y completa (tarjeta con
       «Lucía usa esta ficha en cada mensaje.»).
     - Burbuja: cliente, Lucía (normal y destacada) y mostrador con la etiqueta MOSTRADOR.
     - Bitácora: plegada por defecto; un clic en la «i» de la burbuja de Lucía la despliega
       (pasos ✓/✗, razonamiento en cursiva, tokens y tiempo) y otro la vuelve a plegar.
       Mini resumen de una línea al costado de la burbuja: en Ámbar (barandilla o error) y
       en Ladrillo (derivación).
     - Bloque de turno: sin confirmar (Ámbar), confirmado (Salvia), alquiló (Cobre),
       retiró (Noche), devolvió (apagado), cancelado y no vino (apagados, con su rótulo), y el
       aviso «sin sincronizar con Google Calendar» combinado con cualquiera de esos estados
       (se muestra además del estado, nunca en su lugar).
     - Editor: sin cambios y con cambios (borde Cobre); Deshacer vuelve al valor inicial.
     - Switch: prendido, apagado, con etiqueta y en tamaño chico.
     - Tarjeta de número del día: número en serif con cifras tabulares.
     - Estado vacío: avatar «L», título y texto.
     - Toast: ok (✓) y error (✗), con su acción.
  4. Tipografía, en DevTools sobre /componentes: un nombre en serif computa a Fraunces y un
     texto de cuerpo a Inter; en Network las dos fuentes (woff2 de /_next/static/media)
     responden 200; la Tarjeta de número y las horas tienen font-variant-numeric: tabular-nums.
     Tamaño mínimo de letra (Mateo, 13/9): a 390, en /componentes y en las 8 pestañas, ningún
     texto computa a menos de 14 px. En la consola:
     [...document.querySelectorAll('body *')].filter(e => e.childElementCount === 0 && e.textContent.trim() && parseFloat(getComputedStyle(e).fontSize) < 14).length
     devuelve 0.
  5. En las dos medidas: consola con 0 errores y 0 warnings (no cuentan los mensajes
     informativos del modo dev: [HMR] y el aviso de React DevTools), Network sin respuestas
     4xx/5xx, y a 390 sin scroll horizontal: en la consola,
     [document.documentElement, document.querySelector('main')].every(e => e.scrollWidth <= e.clientWidth)
     devuelve true.
  6. Con `npm run build` + `npm start`, /componentes responde 404: la galería no llega a
     producción.
Evidencia:
Supuestos: (pendientes de pasar a docs/supuestos.md: es compartido y no lo edito)
  - La referencia es DISENO.md más el canvas de Claude Design tal como quedó implementado.
    El canvas no está en el repo, así que sus agregados se aceptan como están: borde-suave
    #F0EBE2, cobre-oscuro #8A5A30, rounded-bloque 6px, los bordes de alquiló (Cobre) y
    retiró (Noche) que DISENO.md pedía en Grafito, y shadow-otto-pop (0 8px 24px) solo para
    capas flotantes (popover, hoja inferior, toast), aunque DISENO.md pide sombra máxima
    0 1px 2px.
  - Tamaño mínimo de letra (decidido por Mateo, 13/9): vale DISENO.md, nunca menos de 14 px
    en mobile. El canvas usa 10–13,5 px en etiquetas y metadatos (174 líneas en 21 archivos):
    se retocan en este hito. En desktop se mantiene la jerarquía del canvas.
  - Sin shadcn/ui, aunque STACK.md § 6 y ARRANQUE.md lo nombran: los 10 componentes son
    propios en Tailwind, y sumar dependencias toca panel/package.json, que es de paneles.
  - /componentes es solo una superficie de verificación: fuera del menú, detrás del login
    como todo el panel, y 404 en el build de producción. Hace falta porque varios estados
    no aparecen en ninguna pestaña (ficha compacta y completa, editor, estado vacío,
    burbuja de mostrador, toast de error y varios estados del bloque de turno).
  - Plegar la bitácora y el mini resumen en Ladrillo completan lo que pide DISENO.md
    § Componentes 4 y el canvas no trajo: hoy la bitácora está siempre abierta, la «i» no
    se puede tocar y el mini resumen solo existe en Ámbar, escrito a mano en ChatThread.
  - Bloque de turno (decisión #2, Mateo 12/9): el componente recibe estado y aviso por
    separado; el aviso se combina con cualquier estado y el enum suma cancelado y no-vino.
    Hasta que paneles migre la base, los valores salen del mock.
