Hito: Prompt de Lucía (H1.3)            Claude Code: agente          Fase: 1
Toca: plantilla-agente/02-prompt.md (completada), scripts/armar-prompt.mjs (nuevo),
  supabase/functions/_shared/prompt.md (generado), supabase/seeds/reglas.sql
Depende de: nada. Fuentes: docs/ficha-del-negocio.md, AGENTE.md § 1, § 5 y § 9,
  docs/otto-bot-notas.md.
Control:
  1. `node scripts/armar-prompt.mjs` genera supabase/functions/_shared/prompt.md con código 0.
     Toma la plantilla + `reglas_agente` + `contexto_agente` (de la base, o de un JSON de
     entrada en los tests): cambiar una regla en la base y volver a correrlo cambia el prompt.
  2. El generador falla (código ≠ 0 y mensaje claro), probado con un archivo de prueba por
     caso, si: queda `{{` o `[[`; el cuerpo pasa las 300 líneas; las reglas no están
     numeradas; la primera línea no nombra a Lucía. Normaliza CRLF → LF y saca los
     comentarios HTML antes de validar.
  3. prompt.md no tiene precios, horarios, duraciones, modelos, links ni nombres de asesores
     (grep de `$`, `\d{1,2}:\d{2}`, `http`, `minutos`): todo eso sale por herramientas
     (principio 2). Solo van textuales la apertura y las frases entre « » de AGENTE.md § 9.
  4. Las reglas del prompt son las 15 de AGENTE.md § 5, numeradas, una por línea. El seed
     `reglas.sql` se reemplaza: las 6 provisorias de Fase 0 pasan a ser esas 15, y lo que en
     ellas era dato (anticipación de 60 a 7 días, un acompañante, 10 min de tolerancia) pasa
     a fragmentos en H1.6. Se aplica a la base real.
  5. El índice de herramientas y de secciones del prompt coincide con AGENTE.md § 4 y § 8.
  6. Nada que cambie turno a turno está en prompt.md (el contexto del turno va aparte, para
     no romper el caché del prefijo).
  7. **Mateo leyó el prompt generado completo y dio el ok** (fecha en Evidencia). Sin ese ok
     no se empieza H1.4.
Evidencia: 13/9 — generado desde la base real; controles 1 a 6 cumplidos. 14/9 — cerrado.
  Mateo dio el ok (control 7) con tres cambios, aplicados donde nacen y no a mano en prompt.md:
  la marca es «Mr Otto», sin punto (presentación en contexto_agente, supabase/seeds/contexto.sql
  y AGENTE.md § 1); el tono, que eran sus notas pegadas tal cual, quedó redactado sin perder nada,
  y la plantilla lo presenta en su propio bloque como texto de la casa, con punto final y un solo
  párrafo aunque el dueño lo cambie desde el panel; el ancla dice «se ajusta a medida, y si hace
  falta se confecciona». En la base, presentacion, tono y ancla_de_valor pasaron a v2 y la v1 de
  cada una quedó en historial_ediciones. Al regenerar cambiaron solo esas líneas y la frase que
  presenta el tono.
  1. `node scripts/armar-prompt.mjs` → código 0: 247 líneas, 15 reglas, nombre "Lucía", contexto
     tono, presentacion y ancla_de_valor. Cambiar una regla cambia una sola línea del prompt, la
     de esa regla (caso 17 de scripts/probar-prompt.js, con JSON de entrada).
  2. `node scripts/probar-prompt.js` → 45/45. Un archivo de prueba por cada rechazo: `{{`, `[[`,
     más de 300 líneas, reglas sin numerar, otro nombre en la primera línea, primera línea sin
     "Sos X,", sin el encabezado de reglas, falta una clave de contexto, reglas vacías. Todos
     salen con código 1, el motivo por stderr y sin escribir nada. Probados CRLF → LF, los
     comentarios HTML, una regla inactiva, una regla con salto de línea y el tono escrito como
     notas (sin punto y con líneas en blanco).
  3. prompt.md: 0 `$`, 0 hh:mm, 0 `http`, 0 "minutos", 0 nombres de asesores. El generador además
     rechaza el prompt entero si aparece un precio, un horario, un link o una duración (casos 7 a
     10), también si llega adentro de una regla de la base (caso 14).
  4. reglas_agente tiene las 15 de AGENTE.md § 5, en segunda persona, aplicadas a la base real el
     13/9 después de una prueba en seco con rollback (15 filas escritas; en la segunda corrida, 0).
     Las 6 provisorias quedaron en historial_ediciones. Lo que en ellas era dato pasa a fragmentos
     en H1.6.
  5. Las 13 herramientas de AGENTE.md § 4 están en el prompt y no nombra ninguna otra; el índice
     de secciones es el de § 8, en el mismo orden (casos 5a a 5e).
  6. Sin fechas; dos corridas seguidas dan el mismo archivo byte a byte. La libreta, los turnos y
     la fecha y hora del turno van aparte, en el contexto que arma H1.7.
  7. Mateo leyó el prompt completo y dio el ok el 14/9.
Supuestos: cómo se publica el prompt (Storage con caché de 60 s, supuesto #3) es de
  logica/paneles; este hito solo lo genera y lo valida.
  Además (van a docs/supuestos.md cuando haya permiso: es un archivo compartido):
  - Si ninguna sección pega, buscar_informacion recibe la sección vacía (null) en vez de un valor
    "no_se". Así el enum de 16 temas es idéntico en la tabla, el schema y el prompt.
  - La confirmación de un turno la arma y la manda el código en un mensaje aparte. Lucía no la
    escribe ni la repite (principio 1).
  - El tono que escribe el dueño manda sobre las líneas genéricas de tono de la plantilla. Las
    reglas no se tocan desde ahí: siguen siendo absolutas.
  - Las reglas van en segunda persona, con voseo, porque todo el prompt le habla a Lucía.
  - El encabezado "REGLAS QUE NUNCA ROMPES" queda sin tilde: es el ancla que buscan el generador,
    el analista y el tester.
  - Cuando haya una fila en prompt_base (paneles, 0015), el generador tiene que leerla en vez de
    la plantilla del repo. Se hace en Fase 2, como anotó paneles en 1.9.
