Hito: Edición del dueño (H1.9)            Claude Code: paneles          Fase: 1
Toca: panel/app/api/** (un handler por tipo de edición), panel/lib/**,
  supabase/migrations/0011_turnos_solapamiento_y_estados.sql,
  supabase/migrations/0012_negocio_agenda.sql, supabase/migrations/0013_negocio_herramientas.sql,
  supabase/seeds/horarios_duraciones.sql
Depende de: logica 1.15 (ficha del cliente editable y policy de UPDATE en Storage para las
  fotos); agente 1.3 (scripts/armar-prompt.mjs, para el prompt base) y 1.4 (nombres de las
  herramientas). Las pantallas son de front (1.2); se conectan en Fase 2.
Control:
  1. Un handler por cada fila de PROCESOS.md § 5: catálogo (modelos, colores, talles,
     precios, fotos), accesorios, horarios y configuración de agenda, fragmentos (activar,
     editar, versión), reglas, contexto y presentación, prompt base, herramientas, notas del
     dueño, fichas de cliente y enlaces.
  2. Cada edición deja su fila en historial_ediciones con la versión anterior y
     editado_por = el usuario logueado. Test: editar dos veces → dos filas de historial.
  3. "Volver a la versión anterior" restaura datos_anteriores y deja, a su vez, su fila.
  4. Prompt base: el handler corre scripts/armar-prompt.mjs y rechaza (4xx con el motivo)
     si no pasa. Test con un prompt que tiene `{{`.
  5. Turnos (decisiones #1 y #2 de Mateo): 0011 agrega EXCLUDE por probador (btree_gist,
     sin contar 'cancelado' ni 'no-vino'), los dos estados nuevos y la columna `aviso`.
     Test SQL: dos turnos que se pisan en el mismo probador → el segundo falla (23P01); si
     el primero está cancelado, el segundo entra.
  6. Agenda (decisión #6): 0012 crea la configuración de agenda (duración por tipo de turno,
     cantidad de probadores, escalonado) con historial; el seed carga los valores de la ficha
     (graduado, novio e invitado 45', doble 90', triple 120', prueba final 15', 3 probadores,
     escalonado de 15'). logica la usa en 1.13.
  7. Herramientas: 0013 crea la tabla de herramientas del agente (nombre, activa,
     descripción) con historial; los nombres coinciden con los de agente 1.4.
  8. Fotos: subir una imagen al bucket catalogo y reemplazarla por la misma ruta funciona.
  9. `npm run typecheck`, `npm run build` y `npm test` limpios.
Evidencia: 13/9 — controles 1, 2, 3, 5, 6, 7, 8 y 9 cumplidos. El 4 pasó contra un doble del
  generador y falta correrlo con el generador real detrás del handler, que Mateo corre al juntar
  las ramas: el hito queda abierto por el 4. 14/9: el handler pasó a --solo-validar, el flag
  real del generador de agente, y el arnés dio otra vez 118/118.
  14/9, decisiones #7 y #9 (docs/decisiones-pendientes-fase1.md): 0030_negocio_franjas_turnos.sql
  con el seed horarios_franjas.sql, probada en una transacción con rollback, corrida dos veces y
  aplicada. franjas_turnos tiene las 7 franjas (lunes a viernes 13–19 con 3 probadores; sábado
  9:30–12 con 3 y 13:30–18:30 con 2; domingo sin filas) y configuracion_agenda.dias_reserva_urgencia
  = 7. La base frena una franja que se pisa con otra del mismo día (23P01), que termina antes de
  empezar o que pide más probadores que la agenda, y que la agenda baje por debajo de una franja.
  Handlers de Configuración › Agenda, todos con historial: POST /api/configuracion/franjas, PATCH y
  DELETE /api/configuracion/franjas/[id], y dias_reserva_urgencia en PATCH /api/configuracion/agenda.
  Borrar una franja deja la versión borrada en el historial con quién la borró, y "volver a la
  versión anterior" la crea de nuevo con el mismo id. GET /api/configuracion y /api/turnos traen
  las franjas. Arnés: 146/146 (28 aserciones nuevas); npm test de logica, todo verde con 0030.
  Cada migración se probó antes en una transacción con rollback, se corrió dos veces
  (idempotente) y recién después se aplicó a la base real: 0011, 0012 con el seed
  horarios_duraciones.sql, 0013, 0014 (enlaces y notas_dueno con versión e historial), 0015
  (prompt_base), 0016 (catálogo con colores, talles y descripción; accesorios con precio_compra)
  y 0017 (autoría e historial).
  1. Un handler por fila de PROCESOS.md § 5, más GET /api/historial y
     POST /api/historial/[id]/restaurar. Todos ejercitados por HTTP con el arnés de 1.8.
  2. Editar dos veces da dos filas de historial con la versión anterior y editado_por = el email
     de la sesión, en modelo, accesorio, regla, nota, enlace y ficha del cliente. editado_por lo
     fija la base (0017): mandarlo en el request da 400, y aunque llegara, la base lo pisa. Con una
     versión vieja, 409 sin escribir.
  3. Volver a la v1 del modelo restaura datos_anteriores (precio 150000, descripción null) y deja
     su propia fila de historial (la v3 que reemplazó). También probado en contexto_agente y en
     prompt_base.
  4. Sin el generador en este checkout, PUT al prompt base da 503 y no guarda nada. El handler
     llama al generador con su contrato real, `node scripts/armar-prompt.mjs --plantilla
     <archivo> --solo-validar`: código 0 si pasa, 1 y los motivos por stderr si no, con las
     reglas y el contexto leídos de la base. Con un doble que sigue ese contrato y rechaza
     cualquier otro flag, un prompt con `{{` da 422 con el motivo y 0 filas, uno válido se
     guarda, y si el generador rechaza una edición sigue activa la versión anterior. Una copia
     del generador real de la rama agente, llamada con esos mismos argumentos desde la raíz,
     rechaza `--validar`, valida la plantilla real contra la base (245 líneas, 15 reglas) y
     rechaza un `{{` sin resolver, sin escribir nada. FALTA: el control con el generador real
     detrás del handler, al juntar las ramas.
  5. Test SQL en transacción: dos turnos que se pisan en el mismo probador dan 23P01. Con el
     primero cancelado o no-vino, el segundo entra. Un turno que empieza cuando termina otro
     entra. 'con-aviso' ya no es un estado (23514) y el aviso vive en su columna.
  6. duraciones_turno (graduado, novio e invitado 45, doble 90, triple 120, prueba_final 15) y
     configuracion_agenda (3 probadores, escalonado 15) cargadas, con historial. El tope de
     turnos.probador sale de la tabla. Bajar probadores con turnos por venir en los que
     desaparecen da 409.
  7. herramientas_agente tiene los 13 nombres de AGENTE.md § 4, la misma lista que el hito 1.4.
     Editar la descripción deja historial; el nombre no se edita (400).
  8. Subir una foto al bucket catalogo da 201. Subirla otra vez por la misma ruta da 200, el
     objeto en Storage pesa lo del archivo nuevo y el link público responde 200.
  9. typecheck y build limpios. Para `npm test` se corrió la versión vigente de la rama logica:
     30/30 con 0011–0019 aplicadas. La copia de tests/sql/run.mjs de esta rama es la de Fase 0 y
     dispararía el worker desplegado, porque no apaga otto.sin_disparo.
  Encontrado y arreglado en 0017: el trigger de historial corría con los permisos de quien edita.
  Como historial_ediciones no deja insertar a authenticated, TODA edición hecha desde el panel
  fallaba por RLS, también la ficha de clientes de 1.15. Los controles de Fase 0 y de 1.15
  editaban como postgres y no lo podían ver. Ahora es security definer.
Supuestos: numeración de migraciones de paneles en Fase 1: 0011–0019. La base es la real y
  compartida: cada migración aplicada la ven los otros roles.
  Además: Configuración y Catálogo los edita solo un admin; Clientes y Conocimiento, cualquier
  perfil aprobado (0007 dejó "solo admin en precios" para paneles; se cambia con `soloAdmin` en
  lib/edicion/entidades.ts). editado_por es el email de la sesión. El prompt base que
  edita el dueño es la plantilla del generador, con {{REGLAS_NUMERADAS}} y {{CONTEXTO:clave}}.
  En Fase 2 el generador tiene que leer el prompt base de prompt_base cuando haya fila, en vez
  de plantilla-agente/02-prompt.md. Donde corra el panel, el generador necesita SUPABASE_DB_URL
  (en local la toma del .env de la raíz). El catálogo cambió de forma en 0016: colores [{nombre, hex}], talles text[] y
  descripcion; color y talle se borraron (la tabla estaba vacía y ningún código las usaba).
  notas_dueno suma `activo`: Lucía debería inyectar solo las activas. Mover o cancelar un turno y
  marcar sus estados no está en PROCESOS.md § 5: va en Fase 2, con Calendar. Se usaron las nueve
  migraciones del rango: no queda número libre para paneles en Fase 1. El 14/9 Mateo abrió el
  0030–0039: 0030 son las franjas de turnos y la reserva de urgencia.
  De 0030: una franja borrada queda en el historial con borrado_por y borrado_at dentro de
  datos_anteriores (historial_ediciones no tiene columnas para eso, y los tres campos de autoría
  siguen siendo los de la versión). Achicar o borrar una franja no toca los turnos ya dados:
  siguen en pie, solo cambia lo que se ofrece. dias_reserva_urgencia va de 1 a 365 y vacío es sin
  reserva (0 no se acepta, para que haya una sola forma de decirlo). El 7 del seed dejó una
  versión de configuracion_agenda en el historial, firmada 'seed'.
