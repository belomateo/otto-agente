Hito: Edición del dueño (H1.9)            Claude Code: paneles          Fase: 1
Toca: panel/app/api/** (un handler por tipo de edición), panel/lib/**,
  supabase/migrations/0011_turnos_solapamiento_y_estados.sql,
  supabase/migrations/0012_negocio_agenda.sql, supabase/migrations/0013_negocio_herramientas.sql,
  supabase/seeds/horarios_duraciones.sql
Depende de: logica 1.15 (ficha del cliente editable y policy de UPDATE en Storage para las
  fotos); agente 1.3 (scripts/armar-prompt.mjs, para el prompt base) y 1.4 (nombres de las
  herramientas). Las pantallas son de front (1.2); se conectan en Fase 2.
Control:
  1. Un handler por cada fila de PROCESOS.md § 5: catálogo (modelos, colores, talles,
     precios, fotos), accesorios, horarios y configuración de agenda, fragmentos (activar,
     editar, versión), reglas, contexto y presentación, prompt base, herramientas, notas del
     dueño, fichas de cliente y enlaces.
  2. Cada edición deja su fila en historial_ediciones con la versión anterior y
     editado_por = el usuario logueado. Test: editar dos veces → dos filas de historial.
  3. "Volver a la versión anterior" restaura datos_anteriores y deja, a su vez, su fila.
  4. Prompt base: el handler corre scripts/armar-prompt.mjs y rechaza (4xx con el motivo)
     si no pasa. Test con un prompt que tiene `{{`.
  5. Turnos (decisiones #1 y #2 de Mateo): 0011 agrega EXCLUDE por probador (btree_gist,
     sin contar 'cancelado' ni 'no-vino'), los dos estados nuevos y la columna `aviso`.
     Test SQL: dos turnos que se pisan en el mismo probador → el segundo falla (23P01); si
     el primero está cancelado, el segundo entra.
  6. Agenda (decisión #6): 0012 crea la configuración de agenda (duración por tipo de turno,
     cantidad de probadores, escalonado) con historial; el seed carga los valores de la ficha
     (graduado, novio e invitado 45', doble 90', triple 120', prueba final 15', 3 probadores,
     escalonado de 15'). logica la usa en 1.13.
  7. Herramientas: 0013 crea la tabla de herramientas del agente (nombre, activa,
     descripción) con historial; los nombres coinciden con los de agente 1.4.
  8. Fotos: subir una imagen al bucket catalogo y reemplazarla por la misma ruta funciona.
  9. `npm run typecheck`, `npm run build` y `npm test` limpios.
Evidencia:
Supuestos: numeración de migraciones de paneles en Fase 1: 0011–0019. La base es la real y
  compartida: cada migración aplicada la ven los otros roles.
