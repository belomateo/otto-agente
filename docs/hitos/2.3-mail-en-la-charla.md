Hito: El mail en la charla y los mensajes preparados (2.3)     Claude Code: agente     Fase: 2 (decisión #17)
Toca: _shared/herramientas/ficha.ts (CAMPOS_FICHA y la validación del mail); _shared/llm/esquemas.ts
  y extractor.ts (el mail en la extracción); _shared/herramientas/guardar_datos_cliente.ts;
  _shared/herramientas/buscar_horarios.ts (el aviso pedir_mail); _shared/turno/contexto.ts (el mail
  en el contexto del turno); _shared/turno/turno.ts (prepararParaEnviar en lugar de enBurbujas);
  plantilla-agente/02-prompt.md → prompt.md; AGENTE.md; tests y guiones
Depende de: 2.2 (prepararParaEnviar, de logica).
Control:
  1. El extractor saca el mail cuando el cliente lo escribe, en cualquier momento de la charla. Se
     valida en código antes de guardarlo: formato, sin espacios y en minúscula. Uno inválido se
     descarta y queda en la bitácora como los otros descartados. Un null nunca borra el que había, y
     uno nuevo y válido reemplaza al anterior.
  2. guardar_datos_cliente también acepta el mail, con la misma validación.
  3. El contexto del turno dice si tenemos el mail o no. Si no lo tenemos, buscar_horarios devuelve
     pedir_mail: true y Lucía lo pide en el mismo mensaje en que ofrece los horarios. Tiene que ser
     antes de agendar, porque después la confirmación de código reemplaza su texto. Lo pide una sola
     vez por charla; si el cliente no lo quiere dar, agenda igual y no insiste (supuesto #35).
  4. El turno arma lo que sale con prepararParaEnviar (2.2) en lugar de enBurbujas: sin «¡» ni «¿»,
     y en 1, 2 o 3 mensajes según el largo. Lo que se guarda en mensajes es exactamente lo que se
     manda. Revisar que la barandilla largo siga teniendo sentido con esto. El worker ya prepara
     lo que sale desde 2.2, así que esto no es urgente: sirve para que el emulador y la charla
     guarden lo mismo desde el turno.
  5. Dos guiones nuevos contra el worker: «reserva con mail», que deja clientes.email en minúscula,
     y «no quiere dar el mail», que agenda igual y lo pide una sola vez. Los 15 de siempre siguen
     pasando contra el worker desplegado, con teléfonos 5490000000xxx sin «+».
  6. tests/herramientas y tests/barandillas verdes, deno check limpio y el prompt en 300 líneas o
     menos.
Evidencia: 15/9 (agente) — controles 1 a 4 y 6 cumplidos; control 5 con los guiones escritos pero
  sin correr contra lo desplegado (ver abajo).
  - Control 1: CAMPOS_FICHA suma "email"; ESQUEMA_FICHA lo pide igual que los demás campos.
    validarExtraccion (extractor.ts) valida el formato ANTES de que nada llegue a actualizarFicha
    (mismo principio que evento/rol/fecha_evento): uno sin forma de mail se descarta a la
    bitácora (`etapa: "extraer", descartados: [...]`, igual que los otros) y nunca pisa lo que
    había. `formatoDeEmailValido` (ficha.ts) es el mismo regex `^[^@\s]+@[^@\s]+\.[^@\s]+$` que el
    check de la base (0029, de logica): hay un test dedicado que lee `pg_get_constraintdef` y
    compara el string contra `FORMATO_EMAIL.source`, así que si algún día se desalinean, el test
    lo dice. Se guarda en minúscula en `actualizarFicha` (mismo lugar donde `nombre` se
    capitaliza): un "Juan@Gmail.com" no se descarta solo por mayúsculas.
  - Control 2: guardar_datos_cliente suma el parámetro `email` al schema; si no tiene forma de
    mail rechaza con `email_invalido` (mismo patrón que `fecha_evento_pasada`) y no toca la ficha.
  - Control 3: buscar_horarios devuelve `pedir_mail: true` cuando hay huecos para ofrecer y
    `ficha.email` es null (si no hay huecos, no lo pide: no hay nada que ofrecer en el mismo
    mensaje). El prompt (LO QUE HACÉS) le dice a Lucía que lo pida en el mismo mensaje en que
    ofrece horarios, una sola vez, y que no insista si no lo quiso dar — apoyado en que ve en el
    historial que ya lo pidió (como el resto de "no preguntes dos veces lo mismo"): no hay una
    marca aparte en la base para esto, no está en el alcance de Toca. `NOMBRE_CAMPO` de
    contexto.ts suma "email": "mail", así que si ya lo tenemos, sale en TU LIBRETA como cualquier
    otro dato conocido.
  - Control 4: turno.ts saca la función local `enBurbujas` y usa `prepararParaEnviar` (de
    `_shared/whatsapp/preparar.ts`, logica 2.2) en los cuatro lugares que armaban
    `mensajesAlCliente`: los textos fijos de derivación (`derivar`), el texto fijo de supuesto
    #33, la rama de barandillas-en-derivación (ahora junta TODAS las piezas que pasaron la
    barandilla y arma UNA sola vez con prepararParaEnviar, en vez de una por pieza — supuesto #36:
    el largo total del turno, no el de cada pieza suelta) y el camino feliz (el texto de Lucía +
    los efectos de código, ej. la confirmación de agendar_turno, combinados en un solo
    prepararParaEnviar). Revisado el barandilla `largo`: sigue teniendo sentido sin cambios — es
    una guarda sobre cómo el MODELO escribe (que parta en párrafos con línea en blanco, tope 600
    por bloque), independiente de en cuántos mensajes de WhatsApp termina saliendo eso después;
    su umbral no choca con los 300/700 de prepararParaEnviar.
  - Control 5: escrito `tests/sql/mail-en-la-charla-desplegado.mjs` (mismo patrón que
    lucia-desplegada.mjs, teléfonos 5490000000092 y 5490000000093 sin «+»), con los dos guiones:
    "reserva con mail" (el cliente lo da al ofrecer horarios, queda en minúscula) y "no quiere dar
    el mail" (agenda igual, el mail queda null). NO se corrió: el worker desplegado no tiene
    deploy access desde este entorno (mismo límite documentado en H2.1 — el conector Supabase de
    acá no ve el proyecto real y no hay CLI local). Queda listo para correr apenas alguien con
    acceso a la cuenta de Sofia redespliegue `worker` desde esta rama.
  - Control 6: `tests/herramientas` 92/92 (suma 6 pruebas nuevas: 2 de buscar_horarios/pedir_mail
    en consultas.test.ts, 2 de guardar_datos_cliente/email en efectos.test.ts, 1 de consistencia
    del regex del mail en enums.test.ts, y el ajuste de turno_barandillas_en_derivacion.test.ts
    por el «¿» que ahora saca prepararParaEnviar). `tests/barandillas` 48/48, sin cambios de
    comportamiento (no se tocó ninguna barandilla). 4 pruebas puras nuevas de `validarExtraccion`
    en `_shared/llm/extractor.test.ts` (no existía un test file para el extractor; ahora sí, para
    el mail). `deno check` limpio en las 60 y pico de `.ts` de `supabase/functions`. Prompt
    regenerado: 256 líneas (bajo las 300). `scripts/probar-prompt.js` 45/45 — hizo falta sumar
    "pedir_mail" a la lista de identificadores permitidos del control 5d (no es una herramienta,
    es un campo que devuelve buscar_horarios, como ya pasaba con "mensaje_al_cliente").
Supuestos: #35 de docs/supuestos.md.
