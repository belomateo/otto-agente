Hito: Esquema pendiente para agente y paneles     Claude Code: logica     Fase: 1
Toca: supabase/migrations/0023_base_ficha_y_motivos.sql, supabase/migrations/0024_fragmentos_temas.sql,
  supabase/migrations/0025_storage_update.sql, tests/sql/**
Depende de: nada. Es hito nuevo (no estaba en TRABAJO.md): lo piden AGENTE.md § 4, § 7 y § 8,
  PROCESOS.md § 4 y el informe del verificador § 5, y ninguna migración de Fase 0 lo cubrió.
  Lo necesitan `agente` (H1.4: guardar_datos_cliente, derivar_a_persona; H1.6: temas) y
  `paneles` (ficha editable y fotos, H1.9), así que va primero entre los hitos de logica.
Control:
  1. `clientes` tiene la ficha de AGENTE.md § 7: evento (casamiento / graduacion / fiesta /
     laboral / otro), fecha_evento, rol (novio / invitado / graduado / padre / otro),
     dia_o_noche, talle_aprox, ciudad, color_preferido, presupuesto_mencionado, notas_libres,
     con check en los que son enum; más version / editado_por / editado_at y el trigger de
     historial, porque el dueño la edita desde Clientes (PROCESOS.md § 5).
  2. `derivaciones.motivo` con check contra el enum de PROCESOS.md § 4: reclamo,
     prenda_danada, corporativo, turno_urgente_sin_hueco, descuento, dato_no_encontrado,
     pide_persona, barandilla_doble, sin_respuesta, timeout.
  3. `fragmentos.tema` con check contra los 16 temas de AGENTE.md § 8.
  4. storage.objects tiene policies de UPDATE en los buckets catalogo y adjuntos para
     perfiles aprobados (reemplazar una foto por la misma ruta deja de dar 403).
  5. Test en tests/sql: un tema, motivo, evento o rol fuera del enum se rechaza (23514) y
     cada valor del enum entra. `npm test` verde con los controles de Fase 0 y los nuevos.
  6. Las migraciones se aplican a la base real y information_schema muestra las columnas y
     los checks nuevos.
Evidencia:
Supuestos: temas en kebab-case y el resto en snake_case (TRABAJO.md § 5). Los datos que
  hoy tiene la base (reglas, horarios, accesorios, contexto) no se tocan.
