Hito: Esqueleto de panel con Auth      Claude Code: logica          Fase: 0
Toca: panel/lib/supabase/**, panel/middleware.ts, panel/app/(auth)/**, panel/components/nav/**
  (solo el pie de usuario y "Salir"), panel/package.json
Depende de: nada (el panel de H1.1/H1.2 ya existía, construido por `front` antes de Fase 0)
Control: abrir el panel en el navegador sin sesión y verificar que redirige a /login; crear
  una cuenta real desde /login y verificar que redirige a /esperando con el perfil en
  estado 'pendiente'; promover ese perfil a aprobado por SQL y verificar que /esperando
  ahora deja pasar a /bandeja mostrando nombre y rol reales en el Sidebar/hoja "Más"; "Salir"
  cierra la sesión de verdad. Consola limpia en cada paso.
Evidencia: probado de punta a punta con Playwright contra el proyecto real: registro →
  fila en `perfiles` (estado=pendiente) y `solicitudes_acceso` creadas por el trigger →
  /esperando → promovido a rol=admin/estado=aprobado por SQL → /bandeja con "QA Fase 0 ·
  Admin" en la navegación → borrado completo del usuario de prueba al terminar (0 filas
  en auth.users/perfiles/solicitudes_acceso). `npm run build` limpio (14 rutas, sin
  errores de tipos).
Supuestos: #18 (el primer admin real no se seedea por INSERT directo: hace falta que
  Mateo se registre una vez y se promueva su fila por SQL — pendiente su email real),
  #19 (mailer_autoconfirm=true: sin confirmación de email, es panel interno y el filtro
  real es la aprobación manual) — en docs/supuestos.md.
