Hito: Lucía en el worker real (2.1)        Claude Code: logica        Fase: 2, adelantado (decisión #15)
Toca: supabase/functions/worker/** (atender.ts: a quién contesta Lucía, la ráfaga y la entrega por
  WhatsApp; index.ts: la conexión directa a Postgres); supabase/migrations/0027_cola_una_por_charla.sql
  (un solo turno por charla, cola_absorber, mensajes.tipo en castellano) y 0028_cola_mostrador.sql
  (responder desde el panel);
  _shared/agenda/calendario_propio.ts (decisión #13); _shared/whatsapp/** (parsear traduce 'text' a
  'texto', enviarImagen); supabase/config.toml (prompt.md viaja con el worker); tests
Depende de: 1.7 (el turno de agente), 1.11 (cola y worker), 1.13 (agenda). Secretos en Supabase,
  cargados el 15/9: OPENAI_API_KEY, LLM_PRINCIPAL, LLM_CLASIFICADOR, LLM_EXTRACTOR y LUCIA_TELEFONOS.
Control:
  1. El worker corre correrTurno de agente contra la base real, con el calendario propio y la
     agenda de 1.13, y manda por Meta lo que devuelve: burbuja por burbuja, cada una con su wamid
     en la charla, y después las fotos del catálogo por su link público.
  2. Contesta solo a los números de LUCIA_TELEFONOS ("*" = todos) y a los ficticios de las pruebas;
     a los demás, nada. Con la charla derivada no contesta. El botón "Confirmo" sigue resuelto en
     código (1.14).
  3. Un solo turno por charla a la vez, y la ráfaga (4 s sin escribir) se contesta junta: los
     trabajos de esos mensajes quedan absorbidos; lo que llega después tiene su propio turno.
  4. Nunca texto libre fuera de la ventana de 24 hs. Si Meta no acepta una burbuja ni con un
     reintento, el turno no se repite: lo que no salió se borra de la charla y queda en la bitácora.
  5. De punta a punta contra lo desplegado, sin Meta: con los datos del cliente, Lucía agenda un
     turno de verdad, adentro de una franja y respetando la reserva de urgencia.
  6. Un mensaje real por WhatsApp (el número de Mateo) recibe respuesta de Lucía y termina en un
     turno en el panel. Necesita que los mensajes lleguen al webhook de Supabase: Meta los manda a
     n8n, y el 15/9 el workspace de n8n ya no existía (404 "No workspace here"): hoy no llegan a
     ningún lado. Hace falta el App Secret para pasar el webhook de Meta a Supabase.
  7. Responder desde el panel (0028): mostrador_enviar guarda el mensaje del equipo en la charla con
     la marca «[mostrador] » y lo encola; el worker lo manda por Meta sin la marca, sin pasar por
     Lucía. Solo con la charla tomada, dentro de las 24 hs y para un usuario aprobado del panel.
Evidencia: 15/9 — controles 1 a 5 y 7 cumplidos; falta el 6.
  - Bug encontrado antes de que lo viera un cliente: el webhook guardaba el tipo de Meta ('text') y
    agruparRafaga solo lee 'texto'. Con un mensaje real de WhatsApp, Lucía no habría encontrado nada
    que contestar. Ahora parsear.ts lo traduce, y 0027 corrigió las 2 filas que había.
  - 0027: probada dos veces en una transacción con rollback y recién después aplicada.
  - deno check limpio en worker, webhook-whatsapp, cron-envios, _shared/whatsapp y _shared/agenda.
    deno test de worker y _shared/whatsapp: 38/38. Los 15 del worker: 12 contra la base, cada uno
    en una transacción con rollback y con el turno y Meta de mentira, y 3 de funciones puras.
    npm test todo verde, con la sección [2.1]: con sesiones reales, un worker no toma el trabajo de
    una charla que otro está atendiendo pero sí el de otra charla; dos a la vez sobre la misma
    charla, la toma uno solo; cola_absorber se lleva el otro texto de la ráfaga y no el botón ni el
    mensaje que llegó después.
  - Desplegados worker (con prompt.md como archivo estático), webhook-whatsapp y cron-envios.
    tests/sql/lucia-desplegada.mjs: 9/9 con el teléfono ficticio 5490000000079. Dos mensajes
    seguidos, una sola respuesta, el segundo trabajo absorbido, nada por Meta (simulado) y 3
    llamadas al LLM en consumo_llm. Reserva: «soy lucas gomez, necesito un turno de invitado para un
    cumpleaños de 15 el 2026-11-14, de tarde» → ofreció el martes 22/9 y el miércoles 23/9 a las
    13:00; «dale, la primera que tengas me sirve» → turno sin confirmar, de invitado, el martes 22/9
    a las 13:00 en el probador 1: adentro de la franja de lunes a viernes 13 a 19 y a 7 días de hoy
    (reserva de urgencia: el evento es en dos meses). Pasó por buscar_horarios y agendar_turno. Sin
    restos en la base.
    tests/sql/envios-desplegado.mjs: 8/8 con el worker nuevo (el botón Confirmo con la charla
    derivada). El webhook desplegado sigue rechazando lo que no viene firmado por Meta (401).
  - Hallazgo para agente: al agendar, Lucía escribió su propia confirmación y además salió la que
    arma agendar_turno en código. El cliente recibe dos «¡Listo, Lucas!». CORREGIDO por agente el
    15/9: barandilla `confirmacion_doble` (`_shared/barandillas/confirmacion_doble.ts`) descarta
    el texto propio del modelo entero cuando agendar_turno o reprogramar_turno salió bien en el
    turno — la confirmación es solo la de código. Ver AGENTE.md § 6 y § 4.
  - Supuesto #33 resuelto por agente el 15/9: a lo que no es texto (foto, audio, ubicación,
    sticker) Lucía le contesta con un texto fijo (`contexto_agente.texto_mensaje_no_soportado`),
    sin pasar por ningún LLM (`rafaga.ts`, campo `soloNoTexto`).
  - 15/9, a la tarde: responder desde el panel (control 7) y los cambios de agente, ya en el worker.
    0028 probada dos veces en una transacción con rollback y aplicada; anon no puede ejecutar
    mostrador_enviar y authenticated sí, con el chequeo de usuario aprobado adentro. npm test
    [2.1]: con Lucía atendiendo no se puede (55000), charla que no existe P0002, texto vacío 22023,
    pasadas 24 hs 55000, un usuario sin aprobar 42501 y anon 42501; con la charla tomada queda en
    la charla con la marca y encolado. deno test de worker y _shared/whatsapp: 40/40 (el mensaje
    del equipo sale sin la marca y con su wamid, sin pasar por Lucía; un ficticio fuera de la lista
    recibe respuesta, sin Meta).
    Worker desplegado con confirmacion_doble y texto_mensaje_no_soportado de agente.
    lucia-desplegada.mjs: 11/11. Al agendar sale una sola confirmación (la de código), y en [3] el
    equipo toma la charla y escribe: el worker desplegado lo manda (simulado) y queda con la marca.
    envios-desplegado.mjs: 8/8; su teléfono pasó a uno que no es de los ficticios, a los que Lucía
    ahora les contesta siempre.
  - 15/9, a la noche — control 6, primera mitad: con el App Secret de Mateo, la suscripción de la
    app de Meta pasó a webhook-whatsapp (decisión #16; token de verificación nuevo, los mismos 10
    campos; la configuración efectiva del número apunta a Supabase). webhook-desplegado.mjs 14/14
    con la firma real. El "hola" que Mateo había mandado a las 16:46 (cuando Meta todavía mandaba
    a n8n) lo reintentó Meta y entró a las 18:24:50; Lucía contestó a las 18:24:58 («Hola Mateo, soy
    Lucía, asistente de Mr Otto. ¿En qué puedo ayudarte hoy?»), salió por Meta con su wamid, con 3
    llamadas al LLM y sin errores. Antes se cerró la charla de prueba del 14/9 (la del stub) para
    que arrancara de cero.
  - Auditoría de Mateo, 15/9: probando la corrección de arriba, el botón «Necesito reprogramar»
    (tipo `button`) caía en la misma rama y Lucía le hubiera contestado que no puede leer fotos —
    un botón no es una foto. CORREGIDO por agente: `rafaga.ts`/`historial.ts` leen `button` como
    texto igual que `texto` (`TIPOS_QUE_SON_TEXTO`; el de «Confirmo» nunca llega hasta acá, se
    resuelve antes en atender.ts). Con esto, reprogramar_turno lo resuelve igual que si lo hubiera
    escrito. Corregido en el branch de agente; todavía no pasó por un redeploy del worker ni por
    guiones contra lo desplegado (`tests/sql/agente-correcciones-desplegado.mjs`, listo para correr).
  Falta para cerrarlo (control 6): una reserva completa por WhatsApp desde el número de Mateo, y
  verla en la pestaña Turnos cuando front la conecte a la base.
Supuestos: #31 a #34 de docs/supuestos.md (la ráfaga, los teléfonos ficticios, lo que no es texto y
  qué pasa si Meta no acepta una burbuja). El worker sigue leyendo WORKER_STUB_TELEFONOS mientras
  LUCIA_TELEFONOS no esté cargada; ese secreto ya se puede borrar.
