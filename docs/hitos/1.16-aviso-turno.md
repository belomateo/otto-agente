Hito: Aviso de turno (H1.16)            Claude Code: paneles          Fase: 1
Toca: supabase/migrations/0031_turnos_aviso_y_confirmacion.sql,
  supabase/seeds/horarios_aviso_turno.sql, panel/lib/queries/avisos.ts,
  panel/lib/edicion/aviso-turno.ts, panel/app/api/turnos/avisos/route.ts,
  panel/app/api/turnos/[id]/ok/route.ts, aviso_turno_min en Configuración › Agenda
  (panel/lib/edicion/entidades.ts, panel/lib/queries/configuracion.ts) y tests/paneles/ (el
  arnés HTTP de paneles, que pasa al repo).
Depende de: decisión #10 de Mateo (15/9). 0011 (estados 'cancelado' y 'no-vino'), 0012 y 0030
  (configuracion_agenda), 0017 (autoría e historial) y 1.15 (ficha del cliente). El cartel y el
  campo de Configuración › Agenda son de front (1.17). confirmado_por = 'cliente' lo escribe
  logica cuando llega el botón "Confirmo" de la plantilla (1.14).
Control:
  1. 0031 agrega turnos.aviso_ok_at, aviso_ok_por y confirmado_por, y
     configuracion_agenda.aviso_turno_min; el seed lo carga en 30. Se prueba en una transacción
     con rollback, se corre dos veces y recién ahí se aplica.
  2. GET /api/turnos/avisos, para cualquier usuario aprobado, trae los turnos con el cartel
     abierto: desde inicio − aviso_turno_min hasta que alguien da OK o termina el turno, sin
     'cancelado' ni 'no-vino', del más próximo al más lejano. Cada uno trae nombre y teléfono
     del cliente, inicio y fin, tipo, probador, evento y su fecha, rol, talle aproximado, color
     preferido, notas, si el cliente ya confirmó por WhatsApp y los links a la charla y a la
     ficha. Test con turnos que empiezan en 10 minutos (sale), en 2 horas (no sale), que
     empezó hace 20 minutos y sigue (sale), que ya terminó, cancelado y no-vino (no salen).
  3. aviso_turno_min se edita en Configuración › Agenda, con historial. Con 180, el turno de
     dentro de 2 horas sale sin tocar código. 0 o vacío dan 400; un 'equipo', 403.
  4. POST /api/turnos/[id]/ok registra aviso_ok_at y aviso_ok_por = el email de la sesión. Si el
     turno seguía 'sin-confirmar', pasa a 'confirmado' con confirmado_at y confirmado_por = ese
     email; si ya lo había confirmado el cliente (confirmado_por = 'cliente'), solo registra el
     OK. Deja su fila en historial_ediciones, y el turno deja de salir en el GET.
  5. Un segundo OK, o dos a la vez, no pisa al primero: da 200 con ya_estaba y el OK sigue
     firmado por quien lo dio. Antes de la ventana, con el turno terminado, cancelado o
     no-vino, 409. Un turno que no existe, 404. Sin sesión, 401.
  6. La firma la pone la base: con la sesión de un usuario, aviso_ok_por y confirmado_por son el
     email de esa sesión aunque el request mande otra cosa (test por la API de Supabase, con
     confirmado_por = 'cliente').
  7. El arnés HTTP de paneles queda en tests/paneles/ y pasa entero. typecheck y build limpios,
     y npm test de logica verde con 0031 aplicada.
Evidencia: 15/9 — cerrado, controles 1 a 7 cumplidos.
  1. 0031 y el seed horarios_aviso_turno.sql se probaron en una transacción con rollback, se
     corrieron dos veces y recién después se aplicaron. configuracion_agenda.aviso_turno_min = 30
     (v3, firmada 'seed'; la v2, sin aviso, quedó en el historial). La prueba en seco también
     cubrió en SQL la vista, el OK, la firma y los permisos, con la sesión de un usuario.
  2. GET /api/turnos/avisos con un 'equipo': de seis turnos del cliente de prueba salen el que
     empezó hace 20 minutos y el de dentro de 10, en ese orden; no el de dentro de 2 horas, el
     terminado, el cancelado ni el no-vino. Cada uno trae inicio y fin ("12:57–13:42"),
     "Invitado · 45’", "Probador 1", nombre, teléfono, evento y fecha, rol, talle, color, notas,
     cliente_confirmo (true en el que confirmó por WhatsApp) y los links a la charla y a la ficha.
  3. Con aviso_turno_min = 180 sale también el de dentro de 2 horas. 0 y vacío dan 400; un
     'equipo', 403. La edición deja historial y volver al 30 funciona.
  4. El OK de un 'equipo' pasa el turno sin confirmar a 'confirmado', con confirmado_at y con
     confirmado_por y aviso_ok_por = su email; deja la v1 en el historial y la v2 firmada. En el
     que confirmó el cliente, confirmado_por sigue siendo 'cliente' y solo se registra el OK.
     Después del OK, ninguno de los dos sale en el GET.
  5. Un segundo OK da 200 con ya_estaba y sigue firmado por el primero. Dos OK a la vez (admin y
     equipo en paralelo): uno confirma, el otro recibe ya_estaba y queda una sola versión nueva.
     Antes de la ventana, cancelado, terminado y no-vino dan 409 con el motivo; un turno que no
     existe, 404; un id mal formado, 400; sin sesión, 401.
  6. Por la API de Supabase, con la sesión de un 'equipo', escribir confirmado_por = 'cliente' y
     aviso_ok_por = 'otra persona' deja las dos con su email, y aviso_ok_at con la hora de la
     base. Sin sesión (como escribe logica) se respeta 'cliente' (prueba en seco).
  7. El arnés quedó en tests/paneles/ (probar-api.mjs, probar-bundle.mjs y generador-doble.mjs),
     con rutas relativas al repo: 163/163, con la limpieza en 0 y las filas reales como estaban.
     Ahora elige un puerto nuevo en cada arranque y reintenta si otro proceso se lo gana, que
     pasó en la primera corrida del 15/9. typecheck y build limpios, la service role no aparece
     en el bundle y el npm test de logica pasó entero con 0031 aplicada.
Supuestos: la ventana es [inicio − aviso_turno_min, fin) y se define una sola vez, en la vista
  turnos_por_avisar: el GET y el OK usan la misma, con la hora de la base. Un OK que llega
  cuando el turno ya terminó (el cartel quedó abierto) da 409, y el cartel igual se cierra en
  el próximo refresco. "Ya lo confirmó el cliente" es confirmado_por con valor: el OK confirma
  solo si el turno sigue 'sin-confirmar' y nadie lo confirmó; en otro estado (alquiló, retiró)
  solo registra el OK. Las notas del cartel son las notas_libres de la ficha. Los links son
  /bandeja/charla?id=(id de su charla más reciente) y /clientes?id=(id del cliente): esas
  pantallas todavía no leen el id y front los conecta en 1.17. Sin charla, el link es null.
  aviso_turno_min va de 1 a 240 en el panel (límite de cordura; la base solo pide ≥ 1). Vacío
  es sin cartel y solo pasa antes del seed, porque el panel no deja vaciarlo. tests/paneles/ no
  está en .claude/hooks/territorios.json: con el hook activo hay que sumarlo a paneles.
