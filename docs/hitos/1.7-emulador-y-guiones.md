Hito: Emulador y 14 guiones (H1.7)            Claude Code: agente          Fase: 1
Toca: supabase/functions/_shared/turno/** (el turno completo de AGENTE.md § 3, pasos 3 a 11:
  agrupar ráfaga, clasificar, armar contexto, LLM principal con tools, validar acciones,
  barandillas, extraer ficha, bitácora), supabase/functions/probar-agente/**,
  scripts/probar-turno.js
Depende de: 1.3 a 1.6. OPENAI_API_KEY y los modelos (LLM_PRINCIPAL, LLM_CLASIFICADOR,
  LLM_EXTRACTOR) en .env — pendiente de Mateo. El worker real de logica (1.11) usa este mismo
  turno en Fase 2.
Control:
  1. `probar-agente` recibe {telefono, mensaje} y corre el turno completo con la misma base,
     sin Meta (la respuesta vuelve en el HTTP) y con Calendar en dry-run.
  2. Los 14 guiones de AGENTE.md § 13 en scripts/probar-turno.js, escritos como escribe un
     cliente desde el celular. Se corren de a uno (una sola charla de prueba a la vez,
     CLAUDE.md § 7), con teléfonos de prueba que se limpian al terminar.
  3. Cada guion se verifica contra la base, no contra lo que dijo Lucía: si dijo que agendó,
     hay fila en turnos; si derivó, hay fila en derivaciones con un motivo del enum; si dio
     un precio, hay consultar_catalogo en eventos_agente de ese turno.
  4. Si el modelo no devuelve texto ni acción, o el turno pasa de 25 s, deriva (principio 8).
  5. Cada turno deja su costo en consumo_llm y sus eventos en eventos_agente.
  6. El subagente tester, en modo agente, corre los 14; su informe queda en
     docs/informes/1-tester-agente.md y distingue falla del agente de falla del dato.
  7. `deno check` limpio.
Evidencia: 15/9 — cerrado (control 6 corrido el 15/9; ver más abajo el informe del tester,
  las dos fallas críticas que encontró YA CORREGIDAS y reconfirmadas, y dos hallazgos propios
  más encontrados al reverificar).

  MODELOS (control previo, con el ok de Mateo del 14/9 y un ajuste más el 15/9): consultados
  los 130 modelos que devuelve `GET /v1/models` para esta clave. La primera elección
  (gpt-6-astra de principal) la bajó Mateo por sobredimensionada; con esa devolución en mente
  se llegó primero a gpt-5.6-terra, y el 15/9 Mateo pidió el más económico posible para los
  cuatro, incluido el principal — quedó así:
    LLM_PRINCIPAL=gpt-5.6-luna — el más barato del catálogo con function calling y salida
      estricta ($1/$6 el millón de tokens). OpenAI lo describe para "detección de intención y
      ruteo", no para sostener una charla con 13 herramientas y 15 reglas — se lo avisé a
      Mateo explícitamente (más rehacer de barandillas, más derivaciones) y decidió igual
      priorizar el costo. Verificado en vivo antes de adoptarlo: llama herramientas bien con
      `reasoning_effort:"none"`, cachea el prefijo igual que gpt-5.6-terra (99%+ en el segundo
      llamado) y sostiene una charla completa con el prompt real razonablemente bien. Un
      hallazgo real de bajar a este modelo está en "BARANDILLA NUEVA" más abajo.
    LLM_CLASIFICADOR=gpt-5.6-luna — ídem principal: OpenAI lo describe puntualmente para
      "detección de intención y ruteo", la tarea exacta del clasificador.
    LLM_EXTRACTOR=gpt-5.4-mini — sin cambios: un escalón de cuidado arriba del clasificador
      para no pisar la ficha con una mala lectura. $0,75/$4,50.
    LLM_ANALISTA=gpt-5.6-sol — grande, corre una vez por día; no era el foco del pedido de
      economía porque el volumen (una corrida diaria) ya lo acota. $5/$30.
  Hallazgo técnico que cambió el diseño del cliente LLM: la API de Responses no cachea el
  prefijo para la familia gpt-5.6 (0% de aciertos en la prueba, incluso con prompt_cache_key
  fijo — bug ya reportado por la comunidad, confirmado acá en vivo). Chat Completions sí cachea
  (99%+ en el segundo llamado), pero exige `reasoning_effort: "none"` junto con `tools` o
  devuelve 400. Por eso _shared/llm/cliente.ts habla con /v1/chat/completions, no con
  /v1/responses, con reasoning_effort "none" en el principal.
  Costo real medido (consumo_llm, con el caché ya tibio): ~$0,002 a $0,003 el turno del
  principal con gpt-5.6-terra; con gpt-5.6-luna, más barato todavía y del mismo orden. El
  primer mensaje de cada charla nueva (sin caché) sale más, del orden de $0,01 a $0,02.
  Clasificador y extractor, ~$0,0005 cada uno por turno.

  BARANDILLA NUEVA, `accesorio_sin_herramienta` (12ª, sumada el 15/9): al bajar
  LLM_PRINCIPAL a gpt-5.6-luna, el hallazgo #1 de abajo (consultar_accesorios sin llamar)
  volvió a pasar pese a estar marcada OBLIGATORIA en el prompt — un modelo más económico
  sigue peor una instrucción de prompt. Es la confirmación en código de la objeción de Mateo
  del 15/9 (código puro para lo determinístico, no confiar en que el LLM se autocontrole):
  se agregó una barandilla de código (_shared/barandillas/accesorio_sin_herramienta.ts) que
  detecta "zapato(s)/cinturón/corbata/camisa" en la respuesta sin `consultar_accesorios` en la
  traza de ese turno, y la rehace. AGENTE.md § 6 y § 4 al día; tests/barandillas: 43/43 (2
  nuevos). Con la barandilla puesta, dos corridas completas de los 15 guiones con
  gpt-5.6-luna de principal: 15/15 las dos veces.

  Sobre la idea de sumar un LLM "validador" aparte (la propuesta original de Mateo del 15/9):
  se le explicó y decidió NO sumarlo. Lo que pedía —que algo confirme que la respuesta del
  principal coincide con lo que las herramientas devolvieron (precio, horario)— ya existe
  desde H1.5 como código puro (`precio_sin_herramienta`, `horario_sin_herramienta`, y ahora
  `accesorio_sin_herramienta`): es gratis, exacto y no puede equivocarse él mismo, a diferencia
  de un modelo validador, que además iría contra el pedido de "lo más económico posible" al
  sumar un llamado pago por turno. Mateo estuvo de acuerdo.

  CÓDIGO (control 1, 2, 3, 4, 5, 7):
  - _shared/llm/: cliente.ts (Chat Completions, 1 reintento con backoff, nunca tira), esquemas.ts
    (JSON estricto del clasificador y del extractor), clasificador.ts, extractor.ts (valida cada
    campo contra su enum/formato antes de escribir: en vivo devolvió "noviembre" como
    fecha_evento en vez de AAAA-MM-DD, y se descarta en vez de romper la fila), principal.ts (el
    bucle de herramientas, máximo 6 iteraciones, corta si una herramienta devuelve
    efectos.cortaTurno), precios.ts (para consumo_llm.costo_usd, no es dato de negocio).
  - _shared/turno/: derivacion_dura.ts (palabra clave en código — reclamo, dañ*, corporativo,
    uniforme — más evento_inminente si la ficha ya tiene la fecha; CLAUDE.md § 2 pone la
    detección por palabra clave en código, así que "corporativo"/"uniforme" explícitos derivan
    ya, sin dejar que el LLM junte los datos primero — eso queda para cuando el cliente lo
    insinúa SIN la palabra exacta, que sigue siendo del LLM, vía la tool, tal cual AGENTE.md
    § 10), rafaga.ts (agrupa lo entrante desde la última respuesta de Lucía; también da el corte
    para que el historial no repita el mensaje de este turno), contexto.ts (libreta + turnos
    activos + hora + horario de hoy), historial.ts, bitacora.ts, turno.ts (la orquesta: pasos 4
    a 11 en orden).
  - supabase/functions/probar-agente/index.ts: recibe {telefono, mensaje}, crea o busca
    cliente/conversación, encola el mensaje, corre correrTurno con calendarioDeEnsayo (dry-run)
    y devuelve el resultado en el HTTP. Corre LOCAL (`deno run`, no desplegado a Supabase): usa
    conexión directa a Postgres con SUPABASE_DB_URL, la misma que tests/herramientas — igual
    patrón que usó `paneles` en H1.8 (arnés contra `next start` local, no contra un deploy).
  - `deno check` limpio en todo `_shared` y en `probar-agente`.
  - Probado en vivo (curl y luego el arnés) de punta a punta: primer mensaje → la apertura
    textual exacta; segundo mensaje de la misma charla → memoria (no se repite ni se
    vuelve a presentar); ficha, eventos_agente y consumo_llm quedan escritos con costo real.

  LOS 15 GUIONES (control 2, 3) — `node scripts/probar-turno.js`, contra el emulador local y
  la base real, con OPENAI_API_KEY real (`env -u OPENAI_API_KEY` primero: esta máquina tiene
  esa variable puesta para Ollama, y sin limpiarla tapa la del .env — no es un bug del
  proyecto, es del entorno). Los 14 de AGENTE.md § 13 más `evento-manana-deriva` (decisión #8).
  Los que necesitan precio siembran 2 modelos de catálogo TEMPORALES (el catálogo real está
  vacío, supuesto #15) y los borran al terminar, mismo patrón que tests/herramientas/_arnes.ts.
  Con gpt-5.6-terra de principal (antes del pedido de economía del 15/9) se corrió varias veces
  mientras se iban encontrando y corrigiendo los bugs de abajo. Con la configuración FINAL
  (gpt-5.6-luna de principal + la barandilla accesorio_sin_herramienta): dos corridas
  completas, 15/15 las dos veces. Base sin restos al terminar (verificado, incluida una charla
  suelta de una prueba manual anterior a scripts/probar-turno.js que se limpió a mano).

  Esas dos corridas de "15/15" fueron ANTES del tester (control 6, más abajo): pasaron limpias
  de casualidad — C1 y C2 ya existían en el código, pero necesitan una frase espontánea o un
  camino del modelo que esas dos corridas no tomaron. El tester sí los encontró, reproducidos en
  vivo. Ver "CONTROL 6 — EL TESTER" y "LOS 15 GUIONES, RECONFIRMACIÓN FINAL" más abajo para el
  resultado real después de corregirlos: ese es el que vale, no este.

  BUGS REALES ENCONTRADOS Y CORREGIDOS, con evidencia en vivo (esto es lo que este hito existe
  para encontrar):
  1. consultar_accesorios no estaba marcada OBLIGATORIA en el prompt (a diferencia de
     consultar_catalogo): Lucía contestó "sí, alquilamos zapatos y cinturón" de memoria, sin
     llamarla. Corregido en plantilla-agente/02-prompt.md y AGENTE.md § 4; prompt regenerado
     (249 líneas, sigue bajo 300; probar-prompt.js 45/45); guion accesorios vuelto a correr:
     ahora sí llama a la herramienta.
  2. El extractor perdía contexto entre turnos: se le pasaba solo el último intercambio, no
     toda la charla. En el guion novio-noche, "evento" pasó de casamiento (dicho en el turno 2)
     a fiesta (adivinado en el turno 3, que no repetía la palabra) porque el extractor no tenía
     forma de saber que ya era un casamiento. Corregido: turno.ts le pasa ahora la charla
     completa hasta ese momento, no solo el último intercambio; el prompt del extractor suma
     "si un dato quedó claro antes y nada lo contradice, seguí completándolo" y "ante la duda
     entre dos categorías, preferí null a adivinar". Vuelto a correr: se mantiene casamiento.
  Los dos son fallas de PROMPT (nunca de la validación en código, que siguió rechazando bien
  cada dato inválido): quedan como ejemplo de qué separa "falla del agente" de "falla del dato"
  (PROCESOS.md § 6) para cuando arranque el analista nocturno.

  Bugs de mi propio arnés de prueba, no del agente (documentados para que quien retome esto no
  los reintroduzca): fechas calculadas en UTC en vez de NEGOCIO_TZ (pasadas las 21 hs en
  Rosario, UTC ya cruzó al día siguiente y el guion comparaba contra la fecha equivocada);
  una fecha de reprogramación que cayó domingo (el local no atiende, y con razón no había
  huecos); guiones que no llegaban a completar los datos obligatorios antes de esperar un
  turno agendado. Los tres corregidos en scripts/probar-turno.js.

  Variación observada, no corregida (es el modelo, no el código): en una corrida de tres,
  ante "cualquiera de las dos me sirve" sin elegir una hora puntual, Lucía derivó en vez de
  tomar la primera opción — se cambió el guion a una elección sin ambigüedad ("la más
  temprano de esas dos") y con eso pasó siempre. Y en pregunta-horarios, en una corrida de
  tres, contestó solo la apertura sin responder la pregunta del mismo mensaje. Ninguna de las
  dos es un bug de código: quedan anotadas para que el tester (control 6) las explore con más
  muestras si hace falta, y como candidatas a un guion nuevo de "elige vos si no elijo yo" en
  la próxima ronda de fragmentos/reglas.

  CONTROL 6 — EL TESTER: corrido en modo agente el 15/9, informe completo en
  docs/informes/1-tester-agente.md (15 guiones base + 20 corridas propias + 8 de investigación
  dirigida). Veredicto: "no apto para producción tal cual está" — encontró **dos fallas
  críticas, las dos de código, las dos reproducidas en vivo dos veces cada una**, más 3 medias,
  2 bajas y 1 hallazgo de datos para otro rol. Detalle completo en el informe; acá, qué se hizo
  con cada una.

  FALLAS CRÍTICAS DEL TESTER — LAS DOS CORREGIDAS Y RECONFIRMADAS:
  - **C1** — el texto libre de `derivar_a_persona` (`mensaje_al_cliente`, lo escribe el modelo)
    salía al cliente sin pasar por ninguna barandilla: en vivo, el tester logró que dijera «el
    sistema no me permite…». Causa: en turno.ts, la rama donde una herramienta corta el turno
    hacía `return` antes del bloque de barandillas. Corregido: esa rama ahora corre cada pieza
    del mensaje por `aplicarBarandillas` (formato + contenido); lo que no sale limpio se
    descarta, y si se descartó algo, se completa con el texto fijo genérico
    (`contexto_agente.texto_derivacion_dura_generica`, insertado en la base real el 15/9 — no
    existía, era un vacío real de H1.4 en adelante, encontrado al escribir la prueba de esto)
    en vez de dejar al cliente sin nada. Reconfirmado con una prueba dirigida
    (`tests/herramientas/turno_barandillas_en_derivacion.test.ts`) que fuerza, con un `fetcher`
    simulado, que el modelo "diga" exactamente la frase peligrosa al derivar: confirma que no
    llega al cliente, que cae al texto fijo, y que queda un evento de bitácora de que
    `menciona_ia` saltó.
  - **C2** — la derivación por evento hoy/mañana no siempre pasaba por el código que la
    garantiza: nada impedía que el modelo llamara a `derivar_a_persona` directo con motivo
    `evento_inminente`, salteando `buscar_horarios`/`agendar_turno` (que son los que guardan la
    fecha en código) y el texto fijo aprobado. En 2 de 3 corridas del tester la fecha se perdía
    y salía un texto improvisado. Corregido: `evento_inminente`, junto con `barandilla_doble`,
    `sin_respuesta` y `timeout` (motivos que en ningún caso decide un modelo — AGENTE.md § 2 y
    § 10), se sacaron del enum que ve la herramienta (`MOTIVOS_DERIVACION_LLM`,
    `_shared/enums.ts`) y además se rechazan en código si igual se intentan
    (`derivar_a_persona.ts`, rechazo `motivo_solo_codigo`). Reconfirmado con una prueba dirigida
    que verifica las dos capas (el schema de `ejecutarHerramienta` Y el código de la
    herramienta sin pasar por el schema).

  HALLAZGO PROPIO #1, al reverificar C2 en vivo con los 15 guiones: cerrada la puerta de
  `evento_inminente`, el guion `evento-manana-deriva` volvió a fallar — el modelo tomó la OTRA
  puerta que seguía abierta, derivando con motivo `turno_urgente_sin_hueco` (un motivo real del
  LLM, para cuando de verdad no hay hueco) SIN haber llamado nunca a `buscar_horarios`: la fecha
  no se guardó y salió un texto propio. Mismo problema de fondo que C2, un escalón más abajo.
  Corregido: `derivar_a_persona` ahora rechaza `turno_urgente_sin_hueco` si `buscar_horarios` no
  está en la traza de este turno (`rechazo: sin_buscar_horarios`) — obliga a pasar por el único
  lugar que de verdad chequea `esEventoInminente` y guarda la fecha. Con el fix,
  `evento-manana-deriva` pasó limpio 2 corridas completas seguidas (ver más abajo).

  HALLAZGO PROPIO #2, al reverificar en vivo: el guion `accesorios` tiró un 500
  («Client has encountered a connection error and is not queryable»): una conexión de Postgres
  que se cae a mitad de turno — ya visto una vez este hito, ver `pool.on('error')` más abajo —
  esta vez rompió en el ÚLTIMO paso del turno (`registrarEventos`, solo bitácora). Como ese paso
  no tenía try/catch, el `throw` DENTRO de un `finally` reemplazó el resultado ya bueno del
  turno (ya calculado, y ya guardado en `mensajes` un paso antes): probar-agente devolvía 500 en
  vez del JSON real, tirando a la basura una respuesta correcta por un fallo de solo-logging.
  Corregido con un try/catch alrededor del paso 11 (turno.ts), igual que ya existía para el
  extractor (paso 10): si falla, se loguea y sigue, nunca tira el resultado del turno.
  Reconfirmado con una prueba dirigida que fuerza esa misma caída con un `db` que envuelve al
  real y rompe una sola vez, justo en ese insert.

  Las dos pruebas dirigidas de los hallazgos propios están en el mismo archivo de C1/C2
  (`turno_barandillas_en_derivacion.test.ts`) y en rechazos.test.ts (el rechazo
  `sin_buscar_horarios`, con su caso parecido). `tests/herramientas` + `tests/barandillas`
  completos: **123/123** (arrancaba en 120 antes de hoy; +3 de estos dos hallazgos).

  FALLAS MEDIAS Y BAJAS DEL TESTER:
  - **M3** (menciona_ia tiene un hueco: "cómo una IA sigue instrucciones" no calza con ninguna
    frase exacta de la lista) — CORREGIDO: se sumó un patrón más amplio (`ia` cerca de una
    palabra de meta-funcionamiento: instrucción, configuración, protege, entrena, responde de
    forma segura), además de las frases exactas que ya había. AGENTE.md § 6 al día.
  - **B1** (mayúscula inconsistente del nombre entre el texto libre y la confirmación armada en
    código) — CORREGIDO: se capitaliza una sola vez, al guardar el nombre en la ficha
    (`_shared/herramientas/ficha.ts`), así todo lo que la lee después ya lo recibe consistente.
  - **M1** (la ficha nunca puede volver a "no sé": `actualizarFicha` trata cualquier `null` como
    "no tocar", así que un dato retractado por el cliente sigue mostrándose con la confianza de
    uno vigente) — NO CORREGIDO, documentado a propósito. Necesita una señal distinta entre "no
    se habló de esto" (no tocar, el caso normal en casi todos los campos) y "se retractó, hay
    que vaciarlo" (un valor centinela que el prompt le enseñe al modelo a mandar solo en ese
    caso) — es un cambio de diseño, no un parche de una línea, y se prefirió documentar la
    limitación antes que apurar el diseño equivocado. Queda para una próxima ronda.
  - **M2** (se presenta dos veces en la misma charla si una pregunta la pone a la defensiva) —
    NO CORREGIDO. Es de prompt, probabilístico (un modelo barato puede volver a fallarlo); el
    tester marcó una barandilla de código liviana como opción (detectar que el texto empieza
    con la apertura textual exacta Y no es el primer mensaje → cortar esa frase) para quien le
    dé prioridad. Queda para una próxima ronda.
  - **B2** (ante "quiero cambiar mi turno" sin tener ninguno, no aclara que no encontró uno) —
    NO CORREGIDO, menor, de prompt. Queda para una próxima ronda.

  HALLAZGO DE DATOS DEL TESTER, PARA MATEO / ROL PANELES, NO ES FALLA DEL AGENTE:
  - **D1** — `franjas_turnos` (la agenda real que usa `buscar_horarios`) no coincide con el
    horario publicado: entre semana solo tiene huecos desde las 13:00 (el horario publicado dice
    10:00 a 19:00), y tiene una franja de domingo (10:00–12:00) aunque el negocio está cerrado
    ese día. El tester encontró evidencia de que son datos de prueba de otro rol y no una carga
    real: las filas en cuestión (más una fila de `horarios` y una duración en
    `duraciones_turno`) comparten el mismo `editado_por` (una cuenta de apariencia de prueba,
    `paneles.admin.mu2ufr1n@example.com`) y quedaron editadas en la misma ventana de un minuto.
    No lo tocamos (es la base compartida de los 4 worktrees, fuera del territorio de `agente`):
    Mateo tiene que confirmar con `paneles` si es intencional o limpiarlo.

  LOS 15 GUIONES, RECONFIRMACIÓN FINAL (control 2, 3) — con la configuración completa del día
  (C1 + C2 + los dos hallazgos propios + M3 + B1), tres corridas completas después del último
  fix:
  - Corrida 1 (antes de los hallazgos propios): 13/15 — `accesorios` tiró 500 (hallazgo propio
    #2) y `evento-manana-deriva` falló (hallazgo propio #1). Las dos causas, corregidas arriba.
  - Corrida 2 (con las dos correcciones puestas): 14/15 — `evento-manana-deriva` YA PASÓ limpio
    (fecha guardada, texto fijo). `reprograma` falló una vez, con silencio en un turno
    ("sin_respuesta"): repetido a mano el mismo intercambio contra el emulador, esa vez agendó
    bien — no reprodujo.
  - Corrida 3: 14/15 — `evento-manana-deriva` volvió a pasar limpio (2/2), `reprograma` pasó
    limpio (confirma que la corrida 2 fue variación del modelo, no una regresión). Esta vez
    `es-caro` falló con silencio en el primer turno; repetido a mano el mismo mensaje, esa vez
    derivó bien con un motivo real (`dato_no_encontrado`) y el texto fijo, no en blanco.
  - Lectura de las tres corridas: los dos hallazgos propios (los que sí eran bugs de código)
    quedaron cerrados y se sostienen (evento-manana-deriva: 2/2 después del fix; el 500 de
    conexión no volvió a aparecer). Lo que queda es una falla silenciosa distinta cada vez
    (`reprograma`, después `es-caro`), que no reprodujo al repetirla a mano: es la variación ya
    avisada a Mateo el 15/9 al elegir gpt-5.6-luna ("más rehacer de barandillas, más
    derivaciones"), no un hueco de código — cuando el modelo no devuelve nada, el turno deriva
    en silencio por diseño (principio 8, control 4 de este hito), con su fila en `derivaciones`
    y motivo real; no es una respuesta perdida sin que nadie se entere, es el camino de
    contención funcionando. Se documenta como variación esperada del modelo económico, no como
    bug pendiente.
  - Base sin restos verificada al terminar: 0 clientes `+549341%`, `catalogo_alquiler` en 0.
    Proceso del emulador matado al terminar.
Supuestos: el turno vive en _shared/ para que el emulador y el worker corran exactamente el
  mismo código (probar-agente es el agente sin Meta, no una copia).
  Además (van a docs/supuestos.md cuando haya permiso: es un archivo compartido):
  - probar-agente corre local, no desplegado a Supabase (ver STACK.md § 3 y § 6 para cuándo
    conviene desplegar de verdad: cuando el worker real de logica lo necesite en Fase 2).
  - Precios de los cuatro modelos hardcodeados en precios.ts (no son dato de negocio, es
    contabilidad interna de costo). Actualizar si cambian los modelos elegidos o el precio de
    OpenAI.
  - "corporativo"/"uniforme" por palabra clave explícita derivan en código sin juntar datos
    antes (CLAUDE.md § 2); la misma intención SIN la palabra exacta la resuelve el LLM juntando
    los datos primero (AGENTE.md § 10). Tensión real entre CLAUDE.md § 2 y la tabla de AGENTE.md
    § 4 que se resolvió a favor de CLAUDE.md § 2 por ser el archivo de mayor autoridad; a
    revisar con Mateo si la intención real era otra.
  - Motivo de derivación cuando salta anuncia_sin_derivar: 'pide_persona' (no hay categoría
    propia en el enum). Dos saltos de barandilla en el mismo turno: 'barandilla_doble', y ahí
    NO se manda el texto que había escrito el modelo (podría traer el problema que hizo saltar
    la barandilla la primera vez): se deriva en silencio, mismo criterio que reclamo.
  - Derivación dura por código (reclamo/prenda_danada/corporativo detectados por palabra clave,
    y sin_respuesta/timeout) usa un texto fijo genérico compartido
    (contexto_agente.texto_derivacion_dura_generica, cargado en la base: "Te paso con alguien
    del equipo para que te ayude con esto. En un rato te escriben."), salvo reclamo que va en
    silencio (converción ya existente para cuando deriva la tool). evento_inminente sigue con
    su propio texto (H1.4).
