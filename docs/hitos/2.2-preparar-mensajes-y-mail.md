Hito: Preparar los mensajes para WhatsApp y el mail en la base (2.2)     Claude Code: logica     Fase: 2 (decisión #17)
Toca: supabase/functions/_shared/whatsapp/preparar.ts y su test; supabase/functions/worker/** (los
  textos fijos del worker pasan por la misma preparación); supabase/migrations/0029_base_email_cliente.sql
  (formato del mail); docs/plantillas-whatsapp.md (borradores sin «¡» ni «¿»)
Depende de: nada. Lo usan 2.3 (agente, en el turno) y 2.4 (paneles, el formato del mail).
Control:
  1. prepararParaEnviar(textos: string[]): string[], una función pura, sin base. Junta los textos
     del turno en orden, separados por una línea en blanco. Saca «¡» y «¿» y limpia los espacios que
     quedan. Después los parte según el largo total: hasta 300 caracteres, 1 mensaje; de 301 a 700,
     2; más de 700, 3, que es el máximo.
  2. Cómo corta: primero en líneas en blanco y, si no alcanza, en fin de oración. Nunca en el medio
     de una oración ni de un link. Las partes quedan parejas. Si no hay dónde cortar, salen menos
     partes, y ninguna parte tiene menos de 40 caracteres.
  3. Tests de cada borde: 300, 301, 700 y 701 caracteres; un texto sin ningún punto de corte; un
     link largo; emojis; «¡» y «¿» en el medio y al principio de una línea; textos vacíos. Aplicarla
     a su propia salida no cambia nada.
  4. El worker pasa por la misma preparación los textos fijos que manda él (texto_turno_confirmado).
     No toca lo que escribe una persona desde el panel (mostrador, supuesto #37).
  5. 0029: chequeo de formato en clientes.email (null permitido), probada dos veces en una
     transacción con rollback. Los mails que ya haya en la base lo cumplen.
  6. Los tres borradores de docs/plantillas-whatsapp.md quedan sin «¡» ni «¿» antes de que Mateo los
     cargue en Meta.
  7. De punta a punta, cuando agente integre 2.3: lucia-desplegada.mjs verifica que ninguna
     respuesta trae «¡» ni «¿» y que ningún turno manda más de 3 mensajes de texto.
Evidencia:
Supuestos: #36 y #37 de docs/supuestos.md.
