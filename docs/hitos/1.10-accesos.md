Hito: Solicitudes de acceso (H1.10)            Claude Code: paneles          Fase: 1
Toca: panel/app/api/accesos/**, panel/lib/queries/accesos.ts
Depende de: 0.2 (registro abierto, trigger del perfil pendiente, /esperando y la guarda del
  middleware: ya funcionan). La pantalla Configuración › Accesos es de front (1.2).
Control:
  1. El handler lista las solicitudes pendientes solo para un admin; un usuario 'equipo'
     recibe 403.
  2. Aprobar: el perfil pasa a 'aprobado' (rol equipo por defecto, el admin puede elegir
     admin) y la solicitud a 'aprobada' con resuelto_por y resuelto_at. Rechazar: solicitud
     'rechazada' y perfil 'rechazado'.
  3. Con dos cuentas (el admin de prueba y una nueva): la nueva ve 0 filas en todas las
     tablas y cae en /esperando; después de aprobarla desde el handler, ve datos y entra a
     /bandeja. La cuenta nueva se borra al terminar.
  4. Nadie puede aprobarse a sí mismo ni cambiarse el rol (test contra la policy de perfiles).
  5. `npm run typecheck` y `npm run build` limpios.
Evidencia: 13/9 — cerrado. Migraciones 0018 y 0019, las dos probadas en una transacción con
  rollback antes de aplicarlas. 0018: resolver_solicitud() aprueba o rechaza en una transacción y
  solo si quien llama es admin; nadie se cambia su propio rol ni estado; el alta de usuario lleva
  on conflict. 0019: borrar la cuenta de un admin que resolvió solicitudes ya no falla, porque
  resuelto_por pasa a null. 0019 salió de la limpieza del arnés: la primera corrida no pudo borrar
  al admin de prueba ("Database error deleting user").
  1. GET /api/accesos con el admin da 200, con nombre, email y hace cuánto. Con un usuario equipo,
     403.
  2. Aprobar deja el perfil aprobado con rol equipo por defecto, y la solicitud aprobada con
     resuelto_por = el admin y resuelto_at. Aprobar dos veces da 409. Rechazar deja la solicitud
     rechazada y el perfil rechazado. Aprobar eligiendo rol admin está probado en SQL; por HTTP se
     probó con el rol por defecto.
  3. La cuenta nueva, pendiente: 403 en las 14 rutas, /bandeja la manda a /esperando y ve 0 filas
     en 23 de las 25 tablas. Solo ve su perfil y su solicitud, que es lo que /esperando necesita.
     Aprobada desde el handler, /api/clientes da 200 con datos y /bandeja da 200. La rechazada da
     403 y va a /esperando. Las tres cuentas temporales se borran al terminar (0 en auth.users y en
     perfiles).
  4. Un pendiente no se aprueba por UPDATE directo (RLS: 0 filas). Un equipo no se da rol admin
     (0 filas). Un admin no se cambia su propio rol (42501, trigger de 0018) ni resuelve su propia
     solicitud (403).
  5. typecheck y build limpios.
Supuestos: el aviso por email al aprobar queda afuera (no hay SMTP propio y la confirmación
  de email está apagada, supuesto #19).
  Además: el "admin de prueba" es una cuenta temporal que crea y borra el arnés, no la de Mateo.
  "La nueva ve 0 filas en todas las tablas" se cumple salvo su propio perfil y su propia
  solicitud, que /esperando necesita leer (así quedó en Fase 0).
