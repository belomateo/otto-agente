Hito: Barandillas (H1.5)            Claude Code: agente          Fase: 1
Toca: supabase/functions/_shared/barandillas/<nombre>.ts (una por barandilla de AGENTE.md § 6:
  sin_markdown, sin_relleno, una_pregunta, largo, precio_sin_herramienta,
  horario_sin_herramienta, deriva_y_pregunta, anuncia_sin_derivar, no_a_secas, menciona_ia,
  fuera_ventana_meta) + index.ts, tests/barandillas/**
Depende de: nada (trabajan sobre el texto de salida y la traza de herramientas del turno).
Control:
  1. Por cada una de las 11: un test que la dispara y otro, con el caso parecido, que NO la
     dispara. Ejemplos: precio_sin_herramienta salta con "$150.000" sin consultar_catalogo en
     la traza y no salta con el mismo texto y la herramienta en la traza, ni con "España 764";
     una_pregunta salta con dos preguntas de cierre y no con una sola. Mínimo 22 tests,
     `deno test tests/barandillas` verde.
  2. Se aplican en orden formato → contenido → reglas; cada una devuelve la acción de su fila
     (limpiar, cortar, rehacer, ejecutar la derivación, bloquear) y el motivo para la bitácora.
  3. Dos saltos en el mismo turno → devuelve "derivar".
  4. Las de formato no llaman a ningún LLM. no_a_secas usa el revisor (LLM_CLASIFICADOR)
     solo si la regla lo pide; en los tests, con un doble.
  5. `deno check` limpio.
Evidencia: 14/9 — cerrado.
  `deno test --no-lock --node-modules-dir=none --allow-read tests/barandillas` → 41/41, sin
  base ni red.
  1. barandillas.test.ts: 27 tests, cada una de las 11 con uno que la dispara y otro con el
     caso parecido que no. Entre otros: precio_sin_herramienta salta con "$150.000" sin la
     herramienta y no con la herramienta en la traza ni con "España 764", y salta también con
     un total armado ($183.500 con 150.000 y 33.500 en la traza, regla 9); una_pregunta salta
     con dos preguntas y no con una ("??" cuenta como una); horario_sin_herramienta salta con
     una hora o un día sin herramienta y no confunde «de 2 a 3 personas» ni «¿a la mañana o a
     la tarde?»; no_a_secas no salta con «no te preocupes»; menciona_ia no salta con «modelos
     de traje».
  2. aplicar.test.ts: van en orden formato → contenido → reglas; cada salto deja barandilla,
     acción de su fila y motivo; las que limpian, cortan o quitan la pregunta arreglan el texto
     y el mensaje sale.
  3. Un salto que ya se rehízo y otro en el mismo turno → "derivar" con motivo
     barandilla_doble.
  4. Las 4 de formato son funciones sincrónicas: no pueden esperar a un LLM. En todo el
     recorrido el revisor (un doble) se llama 0 veces, salvo una vez con una negativa dudosa.
  5. `deno check` limpio.
  consistencia.test.ts: las fórmulas de relleno que prohíbe el prompt están todas en
  sin_relleno, y las 11 de AGENTE.md § 6 son las del código, en el mismo orden y cada una en su
  archivo. Los tests encontraron un error real, ya corregido: «Lamentablemente no tenemos ese
  color» no saltaba, porque el «tenemos» de «no tenemos» contaba como ofrecer algo.
Supuestos: AGENTE.md § 6 las nombra en un solo `_shared/barandillas.ts`; van un archivo por
  barandilla porque así lo piden TRABAJO.md § 5 y ARRANQUE.md.
  Además (van a docs/supuestos.md cuando haya permiso):
  - Un salto es un intento del modelo que hay que rehacer. Si en el primer intento saltan dos,
    se rehace una sola vez con los dos motivos; si el intento rehecho vuelve a saltar, se deriva
    (barandilla_doble). Las que arreglan en código no cuentan como salto.
  - precio_sin_herramienta y horario_sin_herramienta comparan contra lo que devolvieron las
    herramientas en la traza, no solo contra si se llamaron: así también frenan un total sumado
    o una hora distinta de las ofrecidas.
  - no_a_secas decide en código; al revisor le pregunta solo si el mensaje arranca negando y es
    largo u ofrece algo. Sin revisor, en la duda no salta.
  - anuncia_sin_derivar ejecuta la derivación y saca las preguntas; qué motivo se registra lo
    decide el turno (H1.7).
  - AGENTE.md § 6 quedó al día con estos detalles.
