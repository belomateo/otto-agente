Hito: El mail en el panel, la API (2.4)     Claude Code: paneles     Fase: 2 (decisión #17)
Toca: panel/lib/edicion/entidades.ts (clientes.email); panel/lib/queries/ (la ficha de la charla y el
  aviso de turno); tests/paneles/probar-api.mjs
Depende de: 2.2 (el formato del mail en la base, 0029).
Control:
  1. PATCH /api/clientes/{id} con email: se guarda en minúscula y sin espacios. Uno inválido da 400
     con el motivo. Vacío lo borra: lo decide una persona, no Lucía.
  2. La ficha que viene con la charla (GET /api/bandeja/{id}) y el aviso de turno
     (GET /api/turnos/avisos) traen el mail.
  3. Arnés al día con esos casos y la limpieza en 0.
Evidencia: 16/9 — cerrado, controles 1 a 3 cumplidos.
  1. clientes.email (lib/edicion/entidades.ts) normaliza trim + minúscula antes de guardar, lo
     mismo que ya exige la base (0029, clientes_email_formato, rama logica): " Juan.Perez@Gmail.COM "
     → "juan.perez@gmail.com" (200). Un valor sin arroba o sin dominio → 400 con el motivo
     (detalle: campo "email", mensaje "Email inválido"). "" → null (lo borra).
  2. GET /api/bandeja/{id} trae cliente.email (lib/queries/bandeja.ts) y GET /api/turnos/avisos
     trae cliente.email (lib/queries/avisos.ts), los dos ya normalizados.
  3. Arnés HTTP 201/201 (tests/paneles/probar-api.mjs, casos nuevos de PATCH email inválido/vacío/
     válido y de que la charla y el aviso traen el mail); typecheck, build y probar-bundle.mjs
     limpios; npm test de logica en verde. Limpieza en 0.
Supuestos: —
