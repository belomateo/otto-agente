Hito: Webhook → cola → worker con agente stub     Claude Code: logica     Fase: 1
Toca: supabase/functions/webhook-whatsapp/**, supabase/functions/worker/**,
  supabase/functions/_shared/whatsapp/**, supabase/migrations/0020_cola_disparo_worker.sql
  (trigger AFTER INSERT en cola_trabajos → net.http_post al worker + cron de contención
  cada 1 min), tests/sql/**
Depende de: Fase 0 (cola_trabajos, cola_tomar_uno, mensajes.wa_message_id único).
  WA_APP_SECRET en .env (pendiente de Mateo).
Control:
  1. GET de verificación: con WA_VERIFY_TOKEN correcto devuelve hub.challenge; con uno
     incorrecto, 403.
  2. POST con X-Hub-Signature-256 inválida o ausente → 401, cero filas nuevas en la base.
  3. El mismo payload firmado enviado dos veces → 1 fila en `mensajes` y 1 en
     `cola_trabajos` (idempotencia por wa_message_id).
  4. El webhook responde 200 en < 1 s (medido con curl -w, 10 corridas).
  5. Los avisos de estado de Meta (`statuses`: sent/delivered/read) no encolan trabajo.
  6. Un mensaje real al número de Otto entra, el worker lo toma (`cola_trabajos.estado`
     pasa a 'hecho'), el cliente recibe la respuesta del stub y queda el evento en
     `eventos_agente`.
  7. Un trabajo que falla sube `intentos`; al 3er intento queda en 'error' y no se
     reintenta más. Un trabajo trabado en 'procesando' > 5 min lo rescata el cron.
  8. `deno check` limpio en webhook-whatsapp y worker; `npm test` sigue verde.
Evidencia: 13/9 — 0020 aplicada a la base real (antes, prueba en seco con rollback: primera
  vez true, segunda false). webhook-whatsapp y worker desplegadas (verify_jwt = false en
  config.toml; secretos WA_VERIFY_TOKEN, WA_ACCESS_TOKEN, WA_PHONE_NUMBER_ID y WORKER_SECRET
  cargados; project_url y worker_secret en Vault). `deno check` limpio en las dos; `deno test`
  de _shared/whatsapp: 12/12 (firma con el vector de la RFC 4231; payloads de texto, botón,
  foto y estados). `npm test`: 30/30, 9 de 1.11 (registro, dedup, misma conversación
  abierta, derivada sin trabajo, reintentos con tope de 3, rescate de trabados, anon sin
  permiso de ejecución).
  `node tests/sql/webhook-desplegado.mjs` contra lo desplegado, con un App Secret de prueba
  cargado solo durante la corrida y borrado al terminar: 15/15. GET 200 con el challenge y
  403 con otro token; POST sin firma o con otra firma → 401 sin filas; firmado → 200 con 1
  mensaje y 1 trabajo; repetido y aviso de estado sin duplicar ni encolar; latencia en
  caliente con mediana 428 ms y máximo 668 ms (el primer POST, en frío, 1147 ms: la meta
  de < 1 s se cumple en caliente, en frío no); el trabajo pasó a 'hecho' por el disparo
  desde la base y quedó en la bitácora; el worker responde 403 sin su secreto. La base
  quedó sin datos de prueba.
  Controles 1, 2, 3, 4, 5, 7 y 8 cumplidos. Falta el 6 (mensaje real por Meta): necesita el
  App Secret real, el número de Mateo en WORKER_STUB_TELEFONOS y su ok para cambiar la URL
  del webhook en Meta. El hito no se cierra hasta pasar el 6.
  14/9 — la mitad de salida del 6, con un mensaje real: Mateo escribió «hola» al número de
  Otto. Como el webhook de Meta sigue apuntando a n8n, el mensaje (mismo wamid) se tomó de la
  ejecución de n8n y se registró a mano con registrar_mensaje_entrante. El disparo desde la
  base llevó el trabajo al worker en 1,3 s; el worker contestó el stub por Meta, guardó el
  saliente y el evento, y el trabajo quedó 'hecho' 2,8 s después de encolarse. Meta avisó
  sent y delivered al segundo (conversación de servicio, sin cargo). Su número quedó en
  WORKER_STUB_TELEFONOS. Falta la mitad de entrada: que Meta le entregue el mensaje al webhook
  de Supabase con firma (App Secret real + cambio de URL).
  15/9 — decisión #12: Mateo prefiere no cambiar el webhook todavía. El control 6 pasa a cuando
  se conecte el worker real (Fase 2), antes de que Lucía le hable a un cliente; los controles 1
  a 5, 7 y 8 siguen cumplidos.
Supuestos: el agente real lo conecta Fase 2 (acá responde un texto fijo). Desplegar las
  Edge Functions en Supabase es necesario para que Meta pueda llamar al webhook: no es el
  deploy del panel (Fase 3). Autorizado por Mateo el 12/9. Cambiar la URL del webhook
  en Meta (hoy apunta al n8n del prototipo) se hace recién cuando pasen los controles 1–5
  y con el ok de Mateo, porque corta el prototipo.
