Hito: Herramientas de Lucía (H1.4)            Claude Code: agente          Fase: 1
Toca: supabase/functions/_shared/herramientas/<nombre>.ts (una por herramienta de AGENTE.md
  § 4: buscar_informacion, consultar_catalogo, consultar_accesorios, buscar_horarios,
  ver_turnos_cliente, agendar_turno, reprogramar_turno, cancelar_turno, guardar_datos_cliente,
  anotar, enviar_fotos, enviar_link, derivar_a_persona) + index.ts, tests/herramientas/**
Depende de: 1.3 (índice del prompt). De otros roles: logica 1.15 (ficha del cliente y checks
  de enums), logica 1.13 (buscar_horarios llama al cálculo de huecos; hasta que exista, los
  tests usan un doble), paneles 0011/0012 (estados cancelado/no-vino y configuración de
  agenda), logica 1.12 (Calendar; hasta entonces, dry-run).
Control:
  1. Cada herramienta es un archivo con su schema (tool calling), su descripción (lo que lee
     el modelo) y sus precondiciones en código.
  2. Un test por cada rechazo de la tabla de AGENTE.md § 4. Mínimo: agendar_turno (hueco que
     no salió de buscar_horarios en este turno, fuera de horario, cliente con turno activo,
     falta nombre, falta fecha del evento); reprogramar_turno (turno de otro cliente, hueco
     inválido); cancelar_turno (turno de otro cliente); enviar_fotos (más de 3, id que no
     existe); enviar_link (tipo fuera de mapa/resena/web); derivar_a_persona (motivo fuera
     del enum, mensaje con pregunta). `deno test tests/herramientas` verde.
  3. Los enums son idénticos en el schema, en la base y en el índice del prompt: secciones
     (AGENTE.md § 8), motivos de derivación (PROCESOS.md § 4), tipos de turno, tipos de link.
     Un test compara las tres listas.
  4. Ningún hecho lo decide el modelo: horarios salen de 1.13, precios de catalogo_alquiler,
     links de enlaces. grep en _shared/herramientas: sin precios, horarios ni links.
  5. Las de acción escriben lo que dicen (fila en turnos, derivaciones o clientes),
     verificado contra la base en una transacción con rollback. cancelar_turno marca
     'cancelado', no borra.
  6. `deno check` limpio.
Evidencia: 14/9 — cerrado.
  `deno test --no-lock --node-modules-dir=none --allow-net --allow-env --allow-read
  --env-file=.env tests/herramientas` → 71/71, contra la base real: cada prueba corre en una
  transacción con rollback y con fechas de 2030, así no deja nada ni choca con turnos reales.
  1. estatico.test.ts: las 13 son un archivo cada una, con nombre, descripción, schema estricto
     (todo en required, nada de más) y ejecutar con sus precondiciones. index.ts valida los
     argumentos contra el schema en código, atrapa cualquier error sin mostrárselo al cliente
     y deja cada llamada en la traza del turno. definicionesParaElModelo lee herramientas_agente
     (paneles, 0013): una desactivada no se ofrece y la descripción que lee el modelo es la de
     la tabla. Las 13 descripciones del código se pasaron a esa tabla el 14/9 (v2, firmadas
     'seed H1.4 (agente)', con historial de la v1 que había cargado paneles).
  2. rechazos.test.ts, con un test por rechazo y al lado el caso parecido que sí pasa:
     agendar_turno (hueco que no salió de buscar_horarios, o que salió para otro tipo; fuera de
     horario un domingo, en el corte y pasado el cierre, y el que termina justo al cierre entra;
     turno activo, y con el anterior cancelado agenda; falta nombre, y con el de la ficha agenda;
     falta fecha del evento; turno después del evento; evento pasado; fecha_hora sin zona),
     reprogramar_turno (de otro cliente, hueco no ofrecido, fuera de horario, turno no activo),
     cancelar_turno (de otro cliente, id que no existe), enviar_fotos (más de 3, id que no
     existe, sin fotos; con dos que existen manda la primera foto de cada uno), enviar_link
     (pago, mercadopago, instagram), derivar_a_persona (motivo fuera del enum, despedida con
     pregunta; sin pregunta deriva), herramienta que no existe y parámetro de más. Cada rechazo
     verifica que la base quedó igual.
  3. enums.test.ts: secciones = fragmentos_tema_check = índice del prompt = AGENTE.md § 8;
     motivos = derivaciones_motivo_check = PROCESOS.md § 4 (ya con evento_inminente, 0026 de
     logica); tipos de turno de agendar = los de buscar_horarios = turnos_tipo_check = los de
     duraciones_turno; evento, rol y día o noche = los checks de clientes; las 13 herramientas
     de AGENTE.md § 4 = las del código = las de herramientas_agente, con el mismo tipo. Tipos
     de link: el schema = AGENTE.md § 4; en la base no hay lista porque enlaces no tiene columna
     de tipo, así que se reconocen por el nombre y el test exige que ninguno pegue con dos
     enlaces. Falta cargar el link de reseña (dato de Mr Otto): el test lo avisa y no bloquea.
  4. estatico.test.ts barre _shared/herramientas: 0 precios, 0 horarios, 0 duraciones, 0 links.
     Las franjas salen de franjas_turnos, el horario del local de horarios, las duraciones de
     duraciones_turno, los precios de catalogo_alquiler y accesorios_alquiler, los links de
     enlaces y los textos fijos de fragmentos y contexto_agente.
  5. efectos.test.ts: agendar_turno escribe el turno en el primer probador libre (con el 1
     ocupado por otro cliente, cae en el 2), la ficha y la confirmación armada en código
     (fecha y hora, el fragmento del turno en el local y el mapa); si Calendar falla, el turno
     queda en la base con aviso. reprogramar_turno mueve la misma fila (versión 2), vuelve a
     sin confirmar y borra el recordatorio enviado. cancelar_turno marca 'cancelado' con
     motivo y cancelado_at, no borra y libera el hueco. guardar_datos_cliente escribe solo lo
     que vino y deja historial con la versión anterior. anotar deja la nota. derivar_a_persona
     escribe la derivación, deja la charla derivada, no duplica y con descuento o reclamo no
     manda la despedida.
  6. `deno check` limpio en supabase/functions/_shared y tests/herramientas.
  Decisiones del 14/9, también probadas. #8, en inminente.test.ts: con el evento hoy o mañana,
  buscar_horarios deriva en código con motivo evento_inminente y el texto fijo de
  contexto_agente (clave texto_evento_inminente, cargada el 14/9), sin pedirle huecos a la
  agenda; si la agenda devuelve derivar: evento_inminente, lo respeta; pasado mañana sigue el
  camino normal; agendar_turno y reprogramar_turno derivan en vez de escribir; hoy o mañana se
  cuenta en hora de Argentina. #7, en franjas.test.ts: la guarda usa franjas_turnos (ya creada
  por paneles) con la cantidad de probadores de cada franja, y contra la base el probador 3
  del sábado a la tarde se rechaza.
Supuestos: el texto de confirmación de un turno (dirección, mapa, acompañante, tolerancia)
  se arma en código con datos de enlaces y fragmentos, no escrito a mano.
  Además (van a docs/supuestos.md cuando haya permiso: es un archivo compartido):
  - Contrato de la agenda que tiene que cumplir logica en 1.13: huecos({desde, hasta, tipo,
    ahora, fechaEvento}) devuelve {huecos: [{inicio, fin, probador}], derivar?:
    "evento_inminente"}. Hasta que esté, los tests usan un doble con esa forma.
  - agendar_turno no recibe teléfono: el turno es siempre del cliente de la charla.
  - consultar_catalogo no filtra por evento: el catálogo no lo tiene (paneles 0016).
  - guardar_datos_cliente no escribe notas_libres: las notas van por anotar.
  - Los links se reconocen por el nombre en enlaces: "Mapa…", "Reseña…", "Web … alquiler".
    Lo prolijo es una columna tipo con check (mapa, resena, web): queda pedida a paneles.
  - Aunque el hueco venga de la agenda, agendar y reprogramar vuelven a chequear la franja y
    el probador (principio 6).
  - Reprogramar vuelve el turno a sin confirmar y borra recordatorio_enviado_at, así el
    recordatorio sale de nuevo para el horario nuevo.
  - Estas pruebas necesitan `--env-file=.env`. Sumarlas a `npm test` le toca a logica, porque
    package.json es suyo.
