Hito: Cálculo de huecos por probador y duración     Claude Code: logica     Fase: 1
Toca: supabase/functions/_shared/agenda/huecos.ts, huecos.test.ts y huecos_base.test.ts
Depende de: `franjas_turnos` (decisión #7 del 14/9: franjas de turnos por día, cada una con
  su cantidad de probadores; la crea `paneles` en su rango 0030–0039), `configuracion_agenda`
  (escalonado y `dias_reserva_urgencia`, decisión #9) y `duraciones_turno` (0012), la
  restricción EXCLUDE de solapamiento (0011) y el motivo `evento_inminente` (0026, logica).
Control: `deno test` en _shared/agenda con estos casos, todos verdes:
  a. Lunes a viernes: turnos solo de 13 a 19, con los 3 probadores. A la mañana, cero huecos
     aunque el local esté abierto.
  b. Sábado: de 9:30 a 12 con 3 probadores y de 13:30 a 18:30 con 2 (el probador 3 no toma
     turnos a la tarde). Entre las 12 y las 13:30, cero huecos.
  c. Mismo día: solo huecos posteriores a la hora actual (hora de Argentina, NEGOCIO_TZ).
  d. Domingo (sin franjas) y fuera de franja: cero huecos.
  e. Probadores escalonados de a 15' dentro de cada franja (el escalonado sale de la tabla).
  f. Doble (1:30) y triple (2:00) en el mismo probador: nunca cruzan el final de su franja.
  g. Dos clientes piden el mismo hueco, con la agenda real detrás de buscar_horarios y
     agendar_turno de agente: entra uno solo y el otro recibe hueco_ocupado, no un error; al
     volver a buscar, ese hueco ya no se ofrece y siguen las alternativas. (Si llegan en el
     mismo instante, la base rechaza el segundo con la EXCLUDE de paneles, 0011.)
  h. Cambiar una franja, una cantidad de probadores o una duración en la tabla cambia el
     resultado sin tocar código.
  i. Evento hoy o mañana: cero huecos y la respuesta es `derivar: evento_inminente`
     (decisión #8).
  j. Por orden de urgencia (decisión #9): con `dias_reserva_urgencia` = N, a un evento a más
     de N días no se le ofrecen huecos de los próximos N días; a uno dentro de los N días,
     sí, los más cercanos primero. Con N vacío no hay reserva.
  `grep` en huecos.ts: ni una hora, duración, cantidad de probadores ni cantidad de días
  escrita a mano.
Evidencia: 14/9 — cerrado. `deno test --no-lock --node-modules-dir=none --allow-net
  --allow-env --allow-read --env-file=.env supabase/functions/_shared/agenda` → 15/15.
  · 11 sin base (huecos.test.ts): a, b, c, d, e, f, h, i y j con las franjas del 14/9, que
    las fechas de prueba caen el día que dicen, y el grep: en el código de huecos.ts no hay
    ninguna hora "hh:mm" ni otro número que 0, 1 y 2.
  · 4 contra la base real, cada una en una transacción con rollback (huecos_base.test.ts):
    lee franjas, escalonado, reserva y duración de las tablas; un turno tomado no se ofrece y
    uno 'cancelado' o 'no-vino' libera el lugar; cambiar la franja del lunes, una duración y
    la reserva en la tabla cambia el resultado (h); y g con buscar_horarios y agendar_turno
    de agente: los dos clientes ven las 13:00, el primero agenda (probador 1), el segundo
    recibe hueco_ocupado, queda un solo turno a esa hora y al volver a buscar el primero es
    13:15 (probador 2).
  Para enchufarla se trajo la rama agente a logica (merge sin conflictos). Con la agenda ya en
  la rama, los tests de agente siguen verdes: 112/112 (71 de herramientas, 41 de barandillas).
  La base quedó sin datos de prueba y con la agenda del 14/9 intacta.
  Cómo se usa: `agendaDesdeBase(db, tz)` (_shared/agenda/huecos.ts) es la `Agenda` que el
  turno de Lucía y el emulador (agente, H1.7) le pasan a las herramientas en lugar del doble.
Supuestos: los turnos 'cancelado' y 'no-vino' liberan el hueco; los demás estados lo ocupan.
  #20 a #25 de docs/supuestos.md: franja de lunes a viernes sin corte, reserva de urgencia de
  7 días (urgente = evento hasta hoy + N; sin fecha, vale la reserva), qué probadores toman
  turnos en una franja reducida, qué cuenta como "hoy o mañana", nada el día del evento ni
  después, y el escalonado como distancia mínima entre dos arranques.
