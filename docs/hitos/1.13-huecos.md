Hito: Cálculo de huecos por probador y duración     Claude Code: logica     Fase: 1
Toca: supabase/functions/_shared/agenda/huecos.ts, supabase/functions/_shared/agenda/huecos.test.ts
Depende de: `horarios` (Fase 0). Tabla de configuración de agenda — duraciones por tipo,
  cantidad de probadores, escalonado — que crea `paneles` (decisión #6). Restricción
  EXCLUDE de solapamiento (migración de `paneles`, decisión #1).
Control: `deno test` en _shared/agenda con estos casos, todos verdes:
  a. Corte 14–15 para los tres probadores (decisión #3: sin excepción 13–14).
  b. Sábado 9:30–18:30 sin corte.
  c. Mismo día: solo huecos posteriores a la hora actual (hora de Argentina, NEGOCIO_TZ).
  d. Domingo, lunes (sin fila en `horarios`) y fuera de horario: cero huecos.
  e. Tres probadores escalonados de a 15' (el escalonado sale de la tabla).
  f. Doble (1:30) y triple (2:00) en el mismo probador: nunca cruzan el corte ni el cierre.
  g. Dos reservas simultáneas del mismo hueco: la segunda recibe el rechazo de la base
     (EXCLUDE) y la función devuelve "ocupado" con alternativas, no un error.
  h. Cambiar un horario o una duración en la tabla cambia el resultado sin tocar código.
  `grep` en huecos.ts: ni una hora, duración ni cantidad de probadores escrita a mano.
Evidencia:
Supuestos: los turnos 'cancelado' y 'no-vino' liberan el hueco; los demás estados lo ocupan.
