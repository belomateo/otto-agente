// Catálogo › nuevo modelo: POST { modelo, precio_base, descripcion?, colores?, talles?, fotos?, activo? } (H1.9).
import { rutaAlta } from '@/lib/edicion/rutas';

export const POST = rutaAlta('modelos');
